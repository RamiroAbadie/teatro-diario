# ============================================================
# .env.example — versionado, SIN valores reales (D48)
# Copiar a ".env" (ignorado por git) y completar.
# Compose lee .env automáticamente del mismo directorio.
# ============================================================
DB_NAME=teatro
DB_USER=teatro
DB_PASSWORD=cambiame-por-algo-largo-y-aleatorio
