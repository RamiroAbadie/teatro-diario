# Decision Log

> Registro vivo de decisiones (D), supuestos (S), descartes (X) y pendientes (P).
> Regla: nada pasa de S a D sin una decisión explícita. Formato ligero; las decisiones
> arquitectónicas mayores tendrán ADRs propios en `docs/decisions/` cuando lleguemos a ellas.

## Decisiones tomadas

| ID | Decisión | Notas |
|----|----------|-------|
| D1 | Usuario primario: espectador intensivo de teatro de CABA | El ocasional puede usarlo; no se diseña para convertirlo |
| D2 | Núcleo del producto: diario/registro personal | Lo social es capa, no núcleo |
| D3 | Capa social mínima desde el MVP: perfil público + follow + actividad de seguidos + likes a reseñas | Nada más en etapa inicial |
| D4 | Alcance geográfico inicial: CABA | |
| D5 | Gratuito, open source, web primero, móvil futuro | |
| D6 | Sin venta de entradas ni links a venta en el MVP | Link externo: candidato post-MVP |
| D7 | Catálogo cerrado, curado por admin. Usuarios pueden sugerir producciones faltantes vía formulario que genera ficha semi-armada para aprobación | Carga abierta descartada por duplicados/calidad |
| D8 | Sin agenda de funciones. Cada producción tiene estado: `en cartel` / `cerrada` / `próximamente` (estreno o reestreno) | El usuario registra con fecha libre, no contra una función del sistema |
| D9 | Rating: entero de 1 a 10; los promedios se muestran con decimales | |
| D10 | Reseñas de texto en el MVP | |
| D11 | Likes a reseñas: sí en MVP. Comentarios en reseñas: pospuestos post-MVP | Comentarios implican moderación, reportes y notificaciones prematuros |
| D12 | Unidad central del dominio: la **producción** (texto + director + elenco). Distintas producciones del mismo texto son entidades distintas. El cambio de sala no crea una producción nueva | Preliminar; a consolidar en Etapa 2 |
| D13 | La producción lleva campos de texto simples `obra_original` y `autor/dramaturgo` (sin entidad Obra relacionada todavía) | Permite normalizar a entidad Obra en el futuro sin perder el dato |
| D14 | Elenco y equipo como entidades **Persona** reutilizables, con relación Participación (persona, producción, rol). Modelo de la "opción A" con tolerancia operativa de la "C": duplicados ocasionales se aceptan y se corrigen a mano | Habilita la página de artista (versión mínima: nombre + participaciones, sin foto ni bio) |
| D15 | La **Sala** es entidad de catálogo, curada por el admin (~100-200 salas en CABA) | Complejos multi-sala: detalle a resolver en el esquema |
| D16 | La compañía teatral **no** es entidad del MVP | Si hace falta, se menciona en la descripción de la producción |
| D17 | Roles de Participación: lista cerrada inicial — actor/actriz, dirección, dramaturgia. Una persona puede tener múltiples roles en la misma producción | Roles técnicos (iluminación, escenografía, música) quedan post-MVP |
| D18 | El registro es la acción central, en un solo gesto: producción + fecha **opcional/aproximada** + rating opcional + reseña opcional. No existe "rating sin registro" | La fecha difusa resuelve la carga de historial viejo sin caminos especiales |
| D19 | Re-visto permitido: múltiples registros por usuario-producción, cada uno con su rating | |
| D20 | El promedio de una producción se calcula sobre el **último rating de cada usuario**, no AVG plano de todos los registros | Lógica no obvia: dejar explícito en la implementación |
| D21 | Todo el contenido es público. Sin niveles de privacidad en el MVP | Mata una familia entera de complejidad transversal |
| D22 | Home logueado: feed de actividad de seguidos + accesos a "registrar" y "en cartel". Fallback sin seguidos: actividad global | |
| D23 | Búsqueda simple (texto) sobre producciones, personas y usuarios. Salas no se buscan: se navegan desde fichas. Sin motor de relevancia en MVP | |
| D24 | Onboarding mínimo (el usuario queda en la home). Historial viejo: bajo demanda vía sugerencias (D7) — el flujo de sugerencia **entra al MVP**. Mensaje de lanzamiento: "diario de acá en adelante" | Resuelve la tensión catálogo-actual vs diario-histórico sin precarga |
| D25 | **Listas fuera del MVP.** Van a v1.1: el loop cierra sin ellas y no prueban S1 ni S2 | Cierra P5 |
| D26 | Estadísticas del perfil con alcance quirúrgico: obras por año, promedio de ratings propios y poco más. Solo queries sobre registros propios; sin gráficos elaborados | "Tu año en teatro" (Wrapped) queda post-MVP |
| D27 | **MVP congelado** según `docs/product/MVP_SCOPE.md` v1.0. Cambios de alcance requieren decisión explícita en este log | Cierre de Etapa 4 |
| D28 | Cuatro módulos: **Identidad, Catálogo (incluye curaduría), Diario, Social**. Límites de código dentro de una sola aplicación, con interfaz pública explícita por módulo | Ver `docs/architecture/MODULE_MAP.md` |
| D29 | El **feed es composición** en la capa de aplicación (Social aporta el grafo, Diario la actividad, Identidad los nombres), no una capacidad de Social | Corrección surgida de revisión externa |
| D30 | Las referencias por **ID opaco no son dependencias**. Grafo real de dependencias: solo Diario → Catálogo. Autorización = concern transversal de la capa de aplicación | Corrección surgida de revisión externa. ⚠️ **Matizada por D61**: transversal es saber quién sos y si sos admin; que un registro sea tuyo lo hace cumplir Diario, que es quien tiene el dato |
| D31 | Eventos de dominio identificados (`RegistroCreado`, etc.) pero **no implementados**: cero consumidores reales en el MVP. Preparación futura = disciplina de límites, no infraestructura | La discusión de mensajería/brokers se cierra en Etapa 6 |
| D32 | **Monolito modular** como arquitectura inicial. Una app desplegable, cuatro módulos como límites de código, una base relacional con propiedad de tablas por módulo | **ADR-001** |
| D33 | **Sin mensajería asincrónica.** Escalera evolutiva: síncrono in-process → despachador in-process → cola en DB → broker externo; cada peldaño solo ante un problema concreto con evidencia | **ADR-002** |
| D34 | El núcleo de negocio no conoce sus caras: casos de uso invocables por igual desde la web actual y la futura API móvil. Ninguna lógica en controladores ni frontend | Habilita D5 sin rehacer nada |
| D35 | Forma operativa: un proceso + una DB + reverse proxy en **Docker Compose** sobre un único servidor. Sin Kubernetes ni orquestación | Docker justificado por paridad dev-prod y simplicidad de deploy; curva de aprendizaje aceptada |
| D36 | Presupuesto: máximo USD 10/mes. Todo gasto justificado; la alternativa gratuita se descarta solo con evidencia (seguridad, funcionalidad clave, o complejidad que agrega). Asciende a principio P9 | También protege del free tier que cuesta más en horas de lo que ahorra en dólares |
| D37 | Presupuesto de horas: 8 hs/semana totales; hasta 3 hs/semana de curaduría post-lanzamiento, con **sesión fija semanal** (rutina, no "cuando pueda"). Desarrollo ~5-6 hs/semana → MVP estimado en 4-6 meses | El roadmap se escribe contra estos números |
| D38 | Catálogo del día uno: **~50 producciones impecables** (comercial + oficial + independientes grandes). Carga inicial manual: 8-17 hs estimadas | Mejor 50 excelentes que 150 mediocres |
| D39 | Fuente de datos inicial: **carga manual**, con datos fácticos de fuentes públicas y sinopsis de redacción propia. Escalera de fuentes en DATA_STRATEGY.md (scraper → salas → curadores comunitarios), cada peldaño ante un problema real | Difiere la cuestión legal del scraping a si/cuando haga falta |
| D40 | **Firmada.** Modificación del MVP congelado: botón "reportar reseña" + cola de reportes en panel admin + borrado por admin | Primera modificación formal vía D27 |
| D41 | Backend: **Java 21 + Spring Boot** | Mayor solidez del fundador; fricción de arranque mínima; ver STACK.md |
| D42 | Base de datos: **PostgreSQL**. La búsqueda del MVP se resuelve con `pg_trgm` dentro de la base — sin motor de búsqueda aparte | |
| D43 | Frontend: **Next.js (React + SSR)** | **ADR-003** — resuelve P13 (previews/SEO) de fábrica |
| D44 | Auth: Spring Security + sesiones con cookie HTTP-only. JWT recién con la app móvil | D34 lo permite sin rediseño |
| D45 | Operación: VPS chico (~USD 5-6/mes) + Docker Compose (Spring, Next, Postgres, Caddy) + backups `pg_dump` nocturnos a R2 + UptimeRobot + GitHub Actions. Total ~USD 6-7/mes. ⚠️ **Ampliada por D77, no contradicha: el `pg_dump` respalda PostgreSQL y nada más.** Los afiches viven en un volumen del host y necesitan **un segundo backup, propio y separado**; son dos copias distintas con dos restauraciones distintas | Dentro de P9. Backups obligatorios desde el primer usuario real; **las dos restauraciones —base y volumen— se prueban antes de la beta**, no después. Cuando se escribió esta decisión los afiches todavía eran Fase 5 y no existía el volumen: la omisión la encontró la revisión de D77 |
| D46 | Licencia: **AGPL-3.0** | Protege contra clones cerrados tipo SaaS; coherente con producto gratuito y comunitario |
| D47 | **Monorepo**: `/backend`, `/frontend`, `/docs`, compose en raíz | Un solo lugar para issues; features full-stack viajan en un PR; docs junto al código |
| D48 | Secretos: variables de entorno; `.env` local ignorado + `.env.example` versionado; `.gitignore` desde el commit cero. Ninguna credencial en el repo jamás | Cierra P12 |
| D49 | Roadmap en 5 fases (esqueleto que camina → catálogo → diario → social → beta), ~4-6 meses. El deploy es Fase 0, no la última: la deuda operativa se paga primero. Beta cerrada con espectadores reales como pago parcial de P1 | Ver `docs/roadmap/` — cierre de Etapa 10 |
| D50 | 22 historias de usuario con criterios de aceptación (`docs/product/USER_STORIES.md`) + mapa de flujos y pantallas (`docs/product/USER_FLOWS.md`) como fuente de los issues de Fases 1-3. Regla: historia que no mapea a una decisión del log = cambio de alcance (D27) | Cuatro micro-decisiones por defecto (MD-1 a MD-4) atacables en USER_STORIES.md |
| D51 | **Ajuste de ritmo (no de arquitectura), tras sobrecarga real del fundador en Fase 0.** Se congela con condición de reentrada todo lo que no bloquea escribir producto. Los módulos (ADR-001) se mantienen: son estructura de carpetas, no herramienta | R3 se materializó; el plan se ajusta donde es barato |
| D52 | Spring Security **fuera del pom** hasta HU-01 (login, Fase 1) | Reentrada: al implementar auth |
| D53 | **Flyway diferido**: en desarrollo, `ddl-auto: update` (Hibernate crea el esquema desde las entidades). Condición innegociable de reentrada: **antes del primer deploy con datos reales**, migración baseline de Flyway + pasar a `validate` | El drift solo es riesgo con una base de producción que proteger; hoy no existe |
| D54 | Docker local reducido a **un comando**: `docker compose up -d postgres`. Dockerfiles, Caddy, Compose completo y VPS: congelados hasta la fase de deploy (post-catálogo funcionando). Tests pospuestos salvo `ModulithArchitectureTest` (3 líneas, esta semana). Testcontainers vuelve con los tests. CI existente se conserva (mantenimiento cero) | Pierde parte del argumento "deuda riesgosa primero" de D49; aceptado como precio de la motivación, que hoy es la restricción más dura |
| D55 | Frontend fuera de la mesa hasta backend de catálogo funcional. ADR-003 (Next.js) sin cambios; si el fundador quiere rediscutirlo al llegar, se rediscute entonces con decisión explícita. Nota: React Native es mobile — D5 sigue siendo web primero | No decidir en caliente lo que no bloquea hoy |
| D56 | **Reentrada de D52**: Spring Security vuelve con HU-01/02. Sesión con cookie HTTP-only y `SameSite=Lax` (D44), BCrypt, login por email o username. El rol vive en `Usuario` (`USUARIO`/`ADMIN`) y el candado de `/api/admin/**` se define en la capa de aplicación, no en Catálogo. Sin panel de roles: el primer admin se promueve con un `UPDATE` a mano, documentado en el README | El chequeo de admin sigue siendo autorización transversal (MODULE_MAP), así que Catálogo no pasa a depender de Identidad. `secure: true` en la cookie entra en la Fase 5, con HTTPS |
| D57 | CSRF activado desde el día uno: token en cookie legible (`XSRF-TOKEN`) + header `X-XSRF-TOKEN`, sin enmascarado BREACH porque la API nunca renderiza el token en el cuerpo | Se evaluó postergarlo hasta el frontend (Fase 4): se descartó porque la sesión con cookie ya es explotable y activarlo después es más caro que convivir con él en las pruebas por curl |
| D58 | **El "último rating" de D20 es el de la función más reciente**, no el del registro cargado más recientemente: ordena por fecha del registro (los sin fecha al fondo) y desempata por orden de carga. Se resuelve en la base con `DISTINCT ON` de Postgres, en una sola query | La alternativa —último cargado, más simple y portable— rompe el promedio justo en el caso que D24 fomenta: quien sube hoy una salida de 2019 pisaría lo que opinó el mes pasado |
| D59 | **La fecha difusa (MD-1) se guarda normalizada al comienzo de su período** (`marzo de 2023` → `2023-03-01`) junto con la granularidad, y los registros sin fecha viajan en una lista aparte de la respuesta, no mezclados. El orden de MD-2 es **fecha, después precisión, después carga**: la fecha sola no alcanza porque el 1 de enero de 2023 y "2023" a secas se guardan iguales, y ahí el día exacto va primero | Ni se inventan fechas falsas ni se ordena a ojo. La granularidad es además lo que el frontend necesita para mostrar "marzo de 2023" y no "1 de marzo de 2023". El empate lo encontró una revisión externa después de la primera implementación, que ordenaba solo por fecha y carga. El orden termina en el id para que sea total: hay un test con las tres colisiones de granularidad (día-mes, día-año y mes-año) |
| D60 | **Los controladores del diario viven en la capa de aplicación**, no en `diario/internal/` como el resto del proyecto: registrar exige traducir la sesión a un `usuarioId` y firmar reseñas exige usernames, y las dos cosas son de Identidad, de la que Diario no puede depender. Por lo mismo, el promedio y las reseñas se sirven en `/api/producciones/{id}/opiniones` y no adentro de la ficha, que es de Catálogo | Es exactamente el concern transversal que describe MODULE_MAP (autorización + composición). Se evaluó componer una ficha única en la capa de aplicación: se descartó por costo (refactor de todos los DTOs de Catálogo) y porque dos pedidos desde una pantalla no son lógica de negocio en el front (D34). Los casos de uso siguen en el módulo: en los controladores no hay ninguna regla |
| D61 | **La autorización se parte en dos** y MODULE_MAP se corrige para decirlo: la identidad y el rol de admin son transversales (capa de aplicación), pero "este registro es tuyo" lo hace cumplir Diario, que es quien tiene el dato, con el `user_id` que la aplicación le pasa | Surgido de una revisión externa que marcó que el código y el documento no describían lo mismo. Se corrige el documento y no el código: mover el chequeo afuera obliga a exponer el dueño de cada registro en la interfaz pública y a leerlo antes de escribir —dos consultas y una ventana entre una y otra— para quedar más débil que ahora |
| D62 | **El registro guarda una copia del título de la producción**, para que el historial siga siendo legible después de que el admin borre una ficha. Mientras la ficha exista manda el título vivo del catálogo; la respuesta marca con `enCatalogo` si todavía hay adónde ir. **No es fusión de duplicados**: los registros de la ficha borrada siguen apuntando a su id muerto. Fusionar es otra operación y entró aparte, en D63 | También de la revisión externa: el registro huérfano conservaba fecha, puntaje y reseña, y perdía lo único que lo hace un historial. Se descartó impedir el borrado —Catálogo no puede consultar a Diario (MODULE_MAP), así que obliga a mover el DELETE de producciones a la capa de aplicación, y le saca al admin la única herramienta que tiene para limpiar una ficha equivocada— y la baja lógica, que toca todas las consultas del catálogo y agrega un estado paralelo a los de D8. Límite aceptado: la copia es del momento de registrar, así que corregir el título de una ficha y borrarla después deja en el diario el título viejo. Refrescar copias al corregir una ficha necesita que Catálogo avise a Diario, y eso son eventos (D31) |
| D63 | **Firmada.** Ampliación del MVP congelado: el admin puede **fusionar una ficha duplicada en la canónica** (`POST /api/admin/producciones/{id}/fusionar`), como parte de HU-20. Los registros que la duplicada tenga en ese momento pasan a la canónica —con su fecha, su puntaje, su reseña y su id, así los likes de la Fase 3 no se pierden— y la duplicada se borra, todo en una transacción. "En ese momento" es literal: un registro creado en la ventana entre la mudanza y el borrado se escapa, y eso es **riesgo aceptado R9**, no un descuido. La ficha destino no se mezcla con la otra: el admin la deja como quiere y recién ahí fusiona | Segunda modificación formal vía D27, después de que una revisión externa mostrara que D62 dejaba el problema a mitad de camino: el historial sobrevivía legible pero apuntando a una ficha muerta. No hace falta mensajería: es una operación explícita que compone dos módulos síncronamente en la capa de aplicación, con una base y un transaction manager (ADR-002, ARCHITECTURE.md). Backend ahora; el botón y su confirmación van con el panel de la Fase 4/5. Nota honesta: sin usuarios reales no hay historial que rescatar, así que esto no bloqueaba nada hoy — entra ahora porque es barato mientras el código está fresco |
| D64 | **La extensión `pg_trgm` y sus índices GIN entran por un script de inicialización idempotente** (`backend/src/main/resources/db/busqueda.sql`), que Spring corre en cada arranque después de que Hibernate creó las tablas (`defer-datasource-initialization`). No es una migración ni el principio de una: cuando entre Flyway (Fase 5, D53) su contenido se muda a la migración baseline y el archivo y su configuración desaparecen | Hibernate sabe crear tablas desde las entidades, pero no una extensión de Postgres ni un índice `USING gin (... gin_trgm_ops)`, y D53 dejó al proyecto sin el lugar donde eso normalmente vive. Se descartó documentar un `CREATE EXTENSION` a mano en el README: la búsqueda dejaría de funcionar en cualquier base nueva —incluida la de los tests, que es un contenedor efímero— hasta que alguien se acuerde de correrlo, que es exactamente el tipo de paso manual que un proyecto de una persona olvida. Se descartó también adelantar Flyway: la condición de reentrada de D53 es una base de producción con datos, y sigue sin existir |
| D65 | **La búsqueda son tres endpoints y no uno**: `/api/buscar/producciones` y `/api/buscar/personas` los sirve Catálogo, `/api/buscar/usuarios` lo sirve Identidad (D23: cada módulo busca sobre lo suyo). Sin composición en la capa de aplicación. Coincide de tres formas —substring literal, similitud sobre el texto entero (typos) y similitud de la consulta contra el mejor pedazo del texto (el título a medio escribir del autocompletado)— ordenadas por el mejor de los dos puntajes y desempatadas por la similitud sobre el texto entero, con tope de 10 resultados y sin paginado. Consulta vacía devuelve vacío, no el catálogo entero; sin resultados es lista vacía y 200, no 404 | La regla de D60 —componer en la capa de aplicación lo que un módulo no puede conseguir solo— acá no aplica: ninguna de las tres búsquedas necesita datos de otro módulo, así que componerlas sería acoplar tres cosas independientes para ahorrarle dos llamadas a una pantalla que igual las muestra en secciones separadas. Que las tres cuelguen del mismo prefijo de URL no las acopla: la URL no es un límite de módulo. Las tres formas de coincidir están porque cada una tapa un agujero real: con solo similitud sobre el título entero, escribir "vestig" no encuentra "Vestigios de un domingo cualquiera en Villa Crespo" y el autocompletado de HU-09 —el gesto que ES el producto (P8)— queda inservible. El 200 con lista vacía es lo que necesita el estado vacío que deriva a sugerir (HU-08). El desempate lo encontró una revisión externa previa al PR: sin él, "Hamlet", "El Hamlet" y "Hamlet en llamas" empatan en 1 —la consulta entra completa en los tres— y el orden lo terminaba decidiendo el alfabeto, que dejaba segunda a la obra escrita tal cual se la buscó |
| D66 | **El feed se arma al leer, componiendo tres módulos, y pagina por cursor.** La capa de aplicación le pide a Social a quién sigo, a Diario qué registró esa gente y a Identidad cómo se llaman (D29): no hay tabla de feed ni nada materializado. Son tres aportes y **cinco consultas** —la sesión, los seguidos, los registros, los títulos de esas producciones (D62) y los nombres de los autores—, todas por id y ninguna por fila: bajar de ahí pide un join entre tablas de módulos distintos, que es exactamente lo prohibido. Los ítems son registros —con reseña o sin ella— ordenados por **fecha de carga** descendente, que es al revés que el diario (MD-2). La página siguiente se pide con el último ítem entregado (`?cursor=<instante>_<idDelRegistro>`), no con un número de página. El fallback global (D22) es solo para quien no sigue a nadie, y la respuesta lo dice con `global: true`. `/api/feed` es el único `GET` de la API que pide sesión además de `/api/auth/yo` | Se descartó el `offset`: al feed le entran registros por arriba mientras alguien lo scrollea, y cada uno empuja una fila de la página siguiente a mostrarse dos veces — justo en la pantalla donde eso pasa todo el tiempo. Preguntar "lo anterior a este" no puede repetir ni saltear, cuesta una cláusula `where` más y no agrega ninguna herramienta (D51). El cursor lleva el id además del instante porque dos registros pueden compartir microsegundo y sin desempate la página siguiente se comería uno. Ordenar por carga y no por la fecha difusa de la función es lo que hace que el feed muestre lo que la gente está contando ahora: quien sube hoy una salida de 2019 la está contando hoy. Se descartó rellenar con actividad global el feed de quien sigue gente callada: un feed vacío con seguidos es información honesta —los tuyos no registraron nada— y mezclar desconocidos sin avisar hace que no se entienda de quién es lo que se lee. El aviso viaja en la respuesta y no lo adivina la pantalla porque "items vacíos" y "esto es global" son dos cosas distintas El número de consultas quedó escrito porque una revisión previa al PR encontró que decir "tres" —los tres módulos— se leía como tres SQL, y no lo son: la honestidad sobre el costo es lo que va a permitir discutirlo el día que haya volumen. Ninguna se repite por ítem, que es el problema que sí importa. ⚠️ **Ampliada por D68**: con los likes adentro del feed son siete |
| D67 | **Social no valida que las cuentas existan, y seguir/dejar de seguir viven en la capa de aplicación** (misma razón que D60): hacen falta dos ids que el módulo no puede conseguir —el de la sesión y el del username de la URL— y los dos los sabe Identidad, de la que Social no depende (MODULE_MAP). El módulo decide la forma del grafo: nadie se sigue a sí mismo, y seguir dos veces es seguir una. Los contadores y el estado del botón viajan en el perfil (`seguidores`, `seguidos`, y `loSigo` nulo cuando no hay botón que mostrar: sin sesión o en el perfil propio), no en endpoints aparte | Que Social no valide la existencia de las cuentas no es un olvido: sin depender de Identidad no tiene con qué, y quien llama ya resolvió los dos ids para llegar hasta ahí. La idempotencia la sostiene el índice único de la tabla: el chequeo previo evita la excepción en el caso normal, y la carrera de dos clics simultáneos termina en 204 igual que el primero, porque el estado que se pedía ya es el que hay. Se descartó un endpoint propio para los contadores: son tres números que la única pantalla que los muestra ya está pidiendo entera, y separarlos son dos viajes para dibujar una línea. `loSigo` nulo y no `false` para el anónimo distingue "no lo seguís" de "no hay nadie que pueda seguirlo", que es lo que decide si el botón se dibuja |
| D68 | **El like es de Social, cuelga de la reseña y se pregunta siempre por lista** (HU-17, D11). La ruta es `POST`/`DELETE /api/resenias/{id}/like` y el id es el del registro: una reseña es un registro con texto, así que un registro sin texto no es una reseña y da 404 —el 404 normal de algo que no está, no una regla escondida adentro de otro recurso—. El toggle vive en la capa de aplicación por lo mismo que seguir (D60/D67), pero con una diferencia que obliga a más: ahí los dos ids venían resueltos de la sesión y del username, y acá el id llega crudo de la URL, así que Diario expone `existeResenia` y la aplicación lo comprueba antes de escribir. Los contadores viajan donde se leen reseñas —la ficha (`likes` y `leDiLike`, lo que a HU-14 le faltaba) y también el feed—, y las dos consultas que eso cuesta son por lista, nunca una por reseña. **Se puede dar like a la propia reseña. Amplía D66: el feed pasa de cinco consultas a siete.** ⚠️ **Ampliada por D70**: `existeResenia` pasa a ser `autorDeResenia`, porque el botón de reportar necesita saber de quién es | Prohibir el auto-like se evaluó y se descartó (D51): seguirse a uno mismo sí rompía algo concreto —el feed convertido en espejo, HU-16—, y sumarse uno a un contador propio no rompe nada que a esta escala alguien mire; es una regla más para sostener, en dos endpoints y dos respuestas, a cambio de nada. Los likes en el feed no los pedía HU-16, pero el feed es donde más se leen reseñas y un botón sin contador ni estado no se puede dibujar en la Fase 4: se aceptan las dos consultas y quedan escritas acá por el mismo motivo que D66 escribió las cinco. `leDiLike` nulo es "no hay botón" —sin sesión en la ficha, o un ítem sin reseña en el feed— y `false` es "hay botón y está apagado": la misma convención que `loSigo` (D67). Se descartó devolver el contador nuevo en el toggle: responde 204 como el de seguir, y el número actualizado llega con la ficha o la página del feed, que es de donde se tocó el botón. Los likes de una reseña borrada (HU-11) quedan huérfanos, y se acepta igual que los seguimientos de una cuenta borrada: no se muestran —el conteo se pide siempre por reseñas que existen— y no pueden reaparecer pegados a otra, porque la secuencia de ids de registro no reutiliza números. La tabla se llama `like_resenia` porque `LIKE` es palabra reservada de SQL |
| D69 | **La sugerencia la guarda Catálogo con el título como único campo obligatorio, y aprobarla es anotar en qué ficha terminó, no crearla** (HU-08 y HU-21). `POST /api/sugerencias` pide sesión y responde 201 con lo propuesto —esa es la confirmación de recibido, no hay estado que consultar después (MD-3)—. La cola es `GET /api/admin/sugerencias`, de la más vieja a la más nueva y sin paginar, y se vacía por una de dos puertas: `aprobar`, que lleva el `produccionId` de la ficha **que el admin ya cargó** con el formulario de HU-20 precargado, o `rechazar`, que exige un motivo interno. La sala y el elenco son texto libre y no referencias al catálogo; el año es un entero acotado entre 1800 y 2100, que ataja el dedazo sin achicar el historial viejo que D24 quiere rescatar. Los dos controladores viven en la capa de aplicación por la misma razón que los del diario (D60): sugerir traduce la sesión a un `usuarioId` y la cola le pone nombre al id opaco del sugerente, y las dos cosas las sabe Identidad, de la que Catálogo no depende. **Salir de la cola pasa una sola vez, y lo que lo sostiene es que resolver lee la fila bloqueada** (`select ... for update`): el segundo intento espera, vuelve a leer y responde 409 | El orden de la aprobación es lo único que no era obvio, y se eligió el que no pierde propuestas: si la sugerencia saliera de la cola al abrir el formulario, abandonarlo a mitad de camino la borraría de la única lista donde estaba. Con la ficha primero, lo peor que puede pasar es una producción cargada y una sugerencia todavía en la cola, que se resuelve en un clic. Que la comprobación de que esa ficha existe la haga la capa de aplicación y no Catálogo es el mismo patrón del like (D68): quien pregunta ya comprobó. Se descartó que aprobar creara la ficha de una: duplicaría el alta de HU-20 —con participaciones, estados y sala— en un endpoint paralelo, y HU-21 dice literalmente que el formulario que se abre es el de HU-20. Se descartó también buscar duplicados al recibir: comparar el título que alguien tipeó apurado a la salida del teatro contra el catálogo es decidir la identidad de una producción por parecido de texto, que es exactamente lo que el catálogo cerrado (D7) evita; si la obra ya estaba, el admin la rechaza en dos clics y el motivo le queda escrito para la próxima vez que llegue igual. Sin límite de sugerencias por usuario: sin usuarios reales no hay abuso que medir, y el día que lo haya, la cola misma lo muestra. El motivo del rechazo es obligatorio aunque no lo lea nadie más que el fundador — es lo que hace que la misma propuesta rechazada en marzo no se vuelva a analizar de cero en abril. El bloqueo de la fila lo encontró una revisión externa previa al PR: sin él, aprobar y rechazar son leer-comprobar-escribir y dos resoluciones que se solapan pasan las dos el chequeo, así que el "una sola vez" era una promesa del documento que el código no cumplía. Entra un bloqueo pesimista y no una versión optimista porque el conflicto se resuelve cortando y no reintentando: quien llega segundo no tiene nada que volver a intentar. **No es el mismo caso que R9** (D63), que sigue aceptado: allá la carrera es entre dos operaciones distintas —fusionar y registrar— y cerrarla pide coordinar bloqueos entre dos módulos; acá es una fila de una tabla, una anotación, y la diferencia entre cumplir la decisión y no cumplirla |
| D70 | **El reporte es de Social, y moderar borra el texto de la reseña y no el registro** (HU-18 y HU-22, que ejecutan D40). El reporte cuelga de la reseña como el like y por las mismas razones (D68): es una interacción sobre contenido, Social guarda los dos ids opacos sin saber qué son, y la ruta es `POST /api/resenias/{id}/reporte` —motivo opcional y cuerpo opcional, porque el botón es un clic— con 404 si ese registro no tiene texto. No se reporta la propia reseña (HU-18 dice "ajena"): lo comprueba la capa de aplicación, que es la única que conoce la sesión y el autor, y para eso `existeResenia` **se reemplaza por `autorDeResenia`**, que responde las dos preguntas con una consulta —amplía D68—. La cola es `GET /api/admin/reportes`, de la más vieja a la más nueva, sin paginar y con el contexto ya compuesto (texto, autor, obra, puntaje, motivo y quién avisó). **Las dos salidas —`borrar-resenia` y `desestimar`— alcanzan a todos los reportes pendientes de esa reseña, no solo al que se tocó**, con las filas bloqueadas (`select ... for update`) como en D69: el segundo intento responde 409. Borrar deja el registro, su fecha y su puntaje: **el promedio de D20 no se mueve** | Lo ofensivo es lo que alguien escribió, no que haya ido al teatro: borrar el registro entero castigaría la salida además del texto y recalcularía el promedio de una producción por un motivo que no tiene nada que ver con lo que la gente opina de ella. La contra, aceptada: la reseña borrada deja likes huérfanos, que es exactamente el caso que D68 ya aceptaba, y el registro deja de ser reseña, así que desaparece de las reseñas de la ficha y no admite más likes ni reportes — todo eso cae solo de la misma definición ("una reseña es un registro con texto") y no hace falta ninguna regla nueva. **En el feed sigue apareciendo, sin texto**, y eso es correcto y no una omisión: el feed son los registros, con reseña o sin ella (D66), y borrar el texto no borra que esa persona haya ido al teatro. Que el veredicto alcance a todos los reportes de la reseña es lo que evita las dos cosas que rompen una cola: que el admin lea tres veces el mismo texto porque tres personas avisaron, y que borrar deje ítems apuntando a un texto que ya no está. La alternativa —agrupar la cola por reseña— se descartó por MODO ESENCIAL (D51): es una consulta agrupada y una forma que HU-22 no pide, para el mismo resultado. El bloqueo lo toma la consulta que arma el conjunto, y no fila por fila, porque tomarlos de a uno abre un interbloqueo entre dos resoluciones sobre la misma reseña. No hay índice único sobre (reseña, reportante) como en el like: el reporte tiene ciclo de vida, y prohibir el segundo aviso para siempre dejaría afuera el caso legítimo de una reseña editada (HU-11) después de que el primero se desestimara; que no se acumulen pendientes repetidos lo resuelve el servicio con el mismo chequeo previo que el like. `borrarResenia` no lleva `usuarioId` y no chequea dueño: no es el dueño quien borra sino la moderación, y el candado es el de `/api/admin/**` (D61). Es silencioso si el texto ya no está —el autor pudo adelantarse— para que la cola se pueda vaciar igual; por eso mismo la cola muestra `texto: null` en vez de esconder el ítem. Social anota el veredicto pero no lo ejecuta: borrar es de Diario, dueño del dato, y las dos mitades pasan en una transacción de la capa de aplicación, como la fusión (D63). Sin aviso al reportante (MD-3) |
| D71 | **La referencia de interfaz es Letterboxd en lo estructural, corregida por RateYourMusic en la ficha y por Untappd en el gesto** (`docs/product/UI_REFERENCES.md`). Se copian arquitectura de información y patrones de interacción; la identidad visual —paleta, tipografía, logo, nombre (P2)— es propia. Posición en los dos ejes del documento: **a mitad de camino en el eje póster↔tipografía** (la imagen se usa cuando está y la fila tipográfica se sostiene sola cuando no) y **feed-first con el botón de registrar siempre presente** en el eje de jerarquía, que es D22 llevado a pantalla. **Tres cosas se diseñan a mano porque ninguna referencia las tiene**: la ficha sin afiche y su preview OG, el camino del catálogo cerrado (búsqueda vacía → sugerir → confirmación sin promesa de aviso) y "en cartel" como motor de descubrimiento | Copiar patrones probados es economía de la única persona que construye (P3/R-h) y además el usuario objetivo ya los conoce; copiar identidad es otra cosa y no ahorra nada. La corrección tipográfica no es gusto: casi todo el rubro está construido sobre la garantía de una imagen buena y el teatro independiente de CABA no la da (D38/D39), así que una grilla de pósters con datos reales no es un diseño con imágenes faltantes sino un diseño roto. Se descartó el extremo tipográfico completo —más honesto con los afiches, peor con el celular a las 23:30 (P8, Flujo 3 de USER_FLOWS)— y el extremo póster, que queda vivo como alternativa si al cargar las ~50 fichas de D38 el problema del afiche resulta menos grave de lo estimado. Untappd entra solo por el gesto: su catálogo abierto produjo exactamente los duplicados que D7/P5 evitan, y su web débil falla el requisito que decidió ADR-003. Goodreads quedó escrito como anti-referencia: es el destino de un catálogo sin curaduría, dibujado. Los tres huecos son consecuencia directa de decisiones propias (D7, D24, D8/CORE_LOOP), así que no tener a quién copiarles era previsible: son el trabajo de diseño real de la Fase 4 |
| D72 | **La subida de afiches se adelanta de la Fase 5 a la Fase 4**, con el panel de admin. Es lo último que le faltaba a HU-20 del backend (alta con redimensionado, D45) | HU-04 tiene un criterio de aceptación literal —"pegar el link en WhatsApp muestra título + afiche + sinopsis"— y es la mecánica de crecimiento entera (ADR-003, P13). Sin afiches, la Fase 4 no puede cerrar su historia más importante y la pantalla más visible del producto se diseña dos veces: una con placeholder y otra de verdad. La contra es real y se acepta: entra almacenamiento de archivos —disco local con volumen (STACK.md)— una fase antes de lo planeado, y con él un pedazo de la deuda operativa que la Fase 5 iba a concentrar. Se prefiere eso a validar la mecánica de adquisición recién en la última fase, que es cuando ya no hay margen para descubrir que no funciona. No cambia nada del tratamiento de la ficha sin afiche (D71): el afiche opcional lo es igual con la subida hecha, porque el que falta no es el que nadie subió sino el que no existe |
| D73 | **Tailwind CSS es la única dependencia nueva del frontend**: los componentes se escriben a mano, sin librería de UI. Excepción explícita a MODO ESENCIAL (D51), que exige que estas cosas se decidan y no se adopten | El argumento es el mismo con el que Next ganó ADR-003: es lo mejor asistido por IA, y el fundador declara debilidad en frontend con mucho código asistido. Se descartó CSS Modules —cero dependencias, máxima fidelidad a D51— porque es justo donde la asistencia empeora y donde el fundador es más débil: el ahorro de una dependencia se paga en las horas que son la restricción más dura (D37). Se descartó shadcn/ui: sus componentes se copian al repo y no son dependencia real, pero traen estética reconocible y código que nadie escribió, y D71 acaba de decidir que la identidad visual es propia. Los ~8 componentes que se repiten en las 13 pantallas los define `DESIGN_SYSTEM.md`; que sean pocos es lo que hace sostenible escribirlos |
| D74 | **Las URLs públicas del frontend son `/obra/{id}-{slug}`, `/artista/{id}-{slug}` y `/sala/{id}-{slug}`**, con el título slugificado. **El id es lo único que se parsea; el slug es decorativo** y nunca viaja al backend, que sigue siendo todo por id. Si el slug falta o quedó viejo —porque el admin corrigió el título—, la página **redirige `308` a la forma canónica** —el permanente que emite Next, que además preserva el método— en vez de responder distinto. **El id se valida antes de llamar a nada**: un segmento que no empieza con un entero positivo es 404 y no una consulta con basura | Cierra la deuda que el propio código dejó anotada (`CatalogoPublicoController`), donde USER_FLOWS prometía `/obra/{slug}` y la API solo sabe de ids. Con el slug decorativo el link compartido dice de qué obra se trata —que es la mecánica de crecimiento de ADR-003, donde el texto del link es la mitad del preview— y Google indexa por título, todo con cero cambio de backend. Se descartó el slug puro: obliga a Catálogo a guardar un slug único, a decidir qué pasa cuando el título cambia y a mantener redirecciones de los viejos, que es una tabla más y una regla más (D51) para el mismo resultado visible. Se descartó el id pelado: es lo más simple y pierde justo lo que ADR-003 vino a comprar. El redirect permanente y no un `200` con la URL fea: sin él, la misma ficha vive en infinitas URLs —una por typo del slug— y eso es contenido duplicado para Google y links que envejecen mal. La validación del id la encontró la revisión, y la razón por la que se hace con una expresión regular (`/^[1-9]\d*$/`) y no con `Number` no es que `Number` devuelva basura —`Number("hola")` es `NaN` y eso se ve venir, igual que `Number("12abc")`— sino que **acepta de más**: `"12.0"`, `"1e2"`, `"+12"`, `"012"`, `"0x0c"` y `" 12"` son todos números válidos para JavaScript, así que sin la regex la misma ficha respondería `200` en infinitas URLs inventadas, que es exactamente el contenido duplicado que el `308` vino a evitar |
| D75 | **El perfil se sirve en `/usuario/{username}`, no en `/{username}`.** Corrige la forma de MD-4, no su sustancia: el username sigue siendo parte de la URL, URL-safe e inmutable | La raíz colisiona con toda ruta de primer nivel que el producto tenga o vaya a tener —`/buscar`, `/en-cartel`, `/sugerir`, `/login`, `/registro`, `/admin`, `/obra`, `/artista`, `/sala`— y sostenerla obliga a una lista de usernames reservados **validada en el backend**, que hay que acordarse de ampliar cada vez que nace una ruta. La falla de esa lista no es reparable: si mañana el producto quiere `/listas` y ya existe alguien llamado `listas`, o se rompe su perfil o se rompe la ruta, y los links compartidos de MD-4 son para siempre. El costo del prefijo es una URL cinco caracteres más larga y perder el "vanity" al estilo Letterboxd, y se acepta: D71 copia estructura, no la URL. Se descartó conservar la raíz con lista de reservados por lo de arriba, y también porque agrega una validación nueva a Identidad para comprar solamente estética |
| D76 | ✅ **Implementada en el paso 3 de la Fase 4.** **`vecesQueLaVi` entra en `GET /api/producciones/{id}/opiniones`**: `null` sin sesión, `0..N` con sesión. Es lo que cierra HU-10 —"la ficha evidencia que la viste N veces"— y es backend, no frontend | Cruzar el diario en el cliente para contarlo es lógica de negocio en el frontend, que es exactamente lo que D34 prohíbe, y además pide descargar una respuesta que no pagina (el perfil entero) para dibujar un número. Va en `opiniones` y no en la ficha por lo mismo que el promedio y las reseñas: es de Diario y la ficha es de Catálogo (D60), así que la pantalla no suma una tercera llamada — el endpoint ya compone Diario, Identidad y Social. `0` y no `null` para quien tiene sesión y no la vio: son dos estados distintos y los dos dibujan cosas distintas —el CTA de registrar contra "la viste 2 veces"—, la misma convención de `loSigo` y `leDiLike` (D67/D68). Sale del hueco que encontró la revisión de API.md: HU-10 estaba escrita desde la Fase 2 y ningún endpoint la respondía |
| D77 | ✅ **Implementada en el paso 3 de la Fase 4** (el backend; el reparto en Caddy y el montaje del volumen siguen siendo Fase 5). ⚠️ **Enmendada por D88 en un solo punto: la URL pública termina en `.jpg` y no en `.webp`**, porque no existe un escritor de WebP en Java puro. Todo lo demás de esta decisión queda igual, empezando por lo que la sostiene: que una URL de afiche no se reutilice nunca. Su hueco propio —P16, el procesamiento de la imagen— lo cerró D88. **Contrato de afiches** (ejecuta D72): `POST` y `DELETE /api/admin/producciones/{id}/afiche`, multipart, **un solo archivo por producción**, redimensionado y convertido ~~a WebP~~ **a JPEG (D88)** al subir (D45). **Una URL pública de afiche nunca cambia de contenido ni se reutiliza**, tampoco después de reemplazar, borrar y volver a subir — y eso obliga a **dos datos y no uno** en la ficha: `afiche_version`, **contador monótono que arranca en 0, solo sube y nunca se reinicia**, y `afiche_actual`, **la versión publicada hoy o `null` si no tiene afiche** (de ahí sale `aficheUrl`, que es `null` en ese caso, estado normal por D71). La URL pública es **`/afiches/{produccionId}-{version}.jpg` (era `.webp`; la cambia D88), fuera de `/api`**, archivo estático con `Cache-Control: immutable` de un año. **El orden es reservar la versión en la base → escribir el archivo → publicar (`afiche_actual = n`) → borrar el anterior**, y el `DELETE` es el mismo orden invertido: primero `afiche_actual = null`, después borrar el archivo. La regla que lo gobierna: **la base nunca apunta a un archivo que no está**; un archivo que nadie referencia sí se acepta, y los huérfanos que deje un fallo intermedio son basura tolerada. Eso vale también cuando dos operaciones se solapan y no solo cuando una falla: **la reserva es una sola sentencia atómica** (`UPDATE ... RETURNING`, nunca leer-sumar-guardar) y **publicar va con la fila de la producción bloqueada** (`select ... for update`, el patrón de D69/D70), capturando ahí cuál era la versión anterior; **el borrado del archivo viejo pasa DESPUÉS del commit y fuera de la transacción** —borrarlo adentro y que el commit falle deja la base apuntando a un archivo ya borrado, que es la única forma de romper la regla desde el lado que parece más prolijo—, y lo mismo el `DELETE`: `afiche_actual = null` → commit → borrar. El archivo nuevo se escribe en un temporal y se mueve con un movimiento atómico al nombre definitivo. Los tests que esto exige son **concurrentes y deterministas** (dos publicaciones solapadas, y subida solapada con borrado, sincronizadas con un latch): dos operaciones *seguidas* prueban el contador y no el solapamiento. Y como el backup son dos copias de dos momentos distintos, **no alcanza con ordenarlas: hay que parar las mutaciones de afiches durante la ventana** —la rutina nocturna detiene el backend, hace el `pg_dump`, copia el volumen y levanta— y **las dos copias se restauran como una pareja**; con el backend vivo, cualquiera de los dos órdenes puede dejar la base apuntando a un archivo que la otra copia ya no tiene (un reemplazo o un borrado en el medio). ~~**Lo que esta decisión NO decide y bloquea la implementación: con qué se decodifica, redimensiona y codifica la imagen, a qué tamaño, con qué calidad, cómo se trata la orientación EXIF y con qué tope de píxeles decodificados — eso es P16**~~ ✅ **Todo eso lo cerró D88** (TwelveMonkeys para leer, JPEG a la salida, 1200×1600 sin recortar, calidad 0,82, EXIF aplicado en los tres formatos por D89 y tope de 50 MP): ya no bloquea nada. **`DELETE` es idempotente**: `204` aunque la producción ya no tuviera afiche; el `404` es solo para la producción inexistente. En producción el volumen se monta **solo lectura en Caddy y con escritura en Spring**; **en desarrollo Spring escribe en `frontend/public/afiches/` y lo sirve Next como estático** (D78), porque Spring no sirve archivos en ninguno de los dos entornos. El volumen **necesita su propio backup**: el `pg_dump` de D45 no lo cubre. **`aficheUrl` viaja en la ficha y en el resumen de producción, y en ningún otro lado**: no entra en el registro del diario ni en el ítem del feed | La `{version}` es la pieza central: hace que el navegador, WhatsApp y Google vean la imagen nueva después de un reemplazo sin invalidar cachés que no controlamos —las de WhatsApp son justo las que importan (ADR-003)— y es lo que permite el `immutable`, porque una URL de afiche nunca cambia de contenido: se reemplaza por otra URL. **La primera versión de esta decisión no cumplía eso y la revisión lo encontró**: con un solo entero que arrancaba en 1, borrar el afiche y subir otro devolvía la versión 1, y esa URL ya estaba cacheada por un año en máquinas ajenas con otra imagen adentro — el `immutable` pasaba de garantía a mentira. Separar "cuántas versiones hubo" de "cuál está publicada" es el arreglo más chico que lo cierra: el contador sigue siendo un entero que solo sube, y "hoy no tiene afiche" deja de necesitar reiniciarlo. Se descartó un token aleatorio en vez del contador —también evita la reutilización, pero pierde el orden que hace trivial barrer huérfanos y no ahorra ninguna columna— y se descartó guardar solo `afiche_actual` sin contador, que vuelve al mismo agujero por el otro lado: sin memoria de lo ya usado, la próxima versión puede repetir una vieja. Reservar el número en la base **antes** de escribir el archivo es lo que hace la promesa cumplible: si se asignara al publicar, dos subidas que fallan a mitad de camino escribirían las dos sobre el mismo nombre. Publicar en la base recién después de que el archivo esté escrito, y borrar el anterior recién después de publicar, porque el peor resultado posible de un fallo intermedio es un archivo invisible que nadie sabe que existe, contra una ficha apuntando a un archivo que no está; barrer huérfanos comparando el directorio con los `afiche_actual` vivos es fácil el día que pesen, y a 50 fichas (D38) no pesan (D51). El `DELETE` idempotente por la misma razón que el de seguir (D67): el segundo intento pide un estado que ya es el que hay, y responder `404` ahí obligaría a la pantalla a distinguir dos casos que se dibujan igual. Servirlo como estático y no por un endpoint de Spring: es un archivo público (D21), pasarlo por la aplicación cuesta memoria en un VPS chico (P9) y no compra nada. El backup del volumen lo encontró la revisión de este contrato y es una omisión real de D45: sin él, restaurar deja el catálogo entero sin afiches, que es justo lo que ADR-003 necesita para funcionar. Dejar `aficheUrl` afuera de `ProduccionRegistrada` es la parte que se discutió: meterlo obliga a ensanchar `ProduccionBasica`, que es la única superficie de la única dependencia entre módulos del sistema (Diario → Catálogo), para una imagen decorativa que además no existe cuando la ficha fue borrada (D62) — y el feed ya quedó del lado tipográfico del eje de D71. Es reversible si al usarlo se ve pobre, y esa es la única parte de esta decisión que lo es |
| D78 | **La forma del frontend** (`docs/architecture/FRONTEND_ARCHITECTURE.md`), que es al front lo que MODULE_MAP es al backend. Cinco reglas: ninguna lógica de negocio en Next (D34); **el `fetch` vive solo en `lib/api/`**, con un archivo por módulo del backend; Server Component por defecto y `"use client"` como excepción justificada, lo más abajo posible en el árbol; **dos clientes separados, `apiPublic` con TTL y `apiSession` sin caché**, donde **lo que decide es la llamada y no el endpoint** —**dos** `GET` abiertos cambian de respuesta cuando llevan cookie, `opiniones` y el perfil; **el feed no es uno de ellos: exige sesión y va siempre por `apiSession`**, igual que `/api/auth/yo`— y una respuesta pedida con cookie no se cachea nunca; y las mutaciones las hace el navegador contra Spring, **sin Server Actions**. Tres piezas de plomería quedan fijadas ahí y no se improvisan: el **arranque perezoso del token CSRF** antes de la primera mutación, con `GET /api/auth/yo` de semilla y cinco reglas escritas —el `401` es aceptable, se relee la cookie después del GET, la mutación sale recién con token, la semilla no pasa por el manejador global que redirige al login, y el reintento por `403` es uno solo— (sin esto, el visitante que llega por un link compartido y crea una cuenta se come un `403`, que es el Flujo 1 entero); el **rewrite local de `/api`**, que le da a desarrollo el mismo origen único que Caddy da en producción, **con `/afiches` servido en desarrollo por Next desde `public/`** y no por un rewrite, porque Spring no sirve estáticos (D77); y el **redirect `308` canónico con el id del slug validado con una regex** antes de llamar a nada. ⚠️ **Pendiente de comprobación**: que el `401` de `/api/auth/yo` traiga `Set-Cookie: XSRF-TOKEN` está respaldado por la cadena de filtros de Spring Security (el `CsrfFilter` corre antes de la autorización y el token se resuelve sin carga diferida por `setCsrfRequestAttributeName(null)`), pero **falta el `curl` contra el backend corriendo**; hasta entonces esa pieza está especificada y no verificada, con `GET /api/en-cartel` como reemplazo ya identificado | Escribirlo antes de la primera pantalla es lo mismo que hizo rápida la Fase 1: cuando aparece algo imprevisto no hay que decidir dónde ponerlo, y si no encaja en ninguna carpeta es que hay algo que pensar. Que `lib/api/` espeje los módulos es ese mecanismo de aviso. Los dos clientes están separados porque mezclarlos produce el peor bug posible de esta aplicación y además silencioso: con todo el contenido público (D21), servir la home de una persona a otra se ve como una página válida con los datos de otro, no como un error. Se descartaron las Server Actions: agregan un salto que igual tiene que reenviar cookie y token CSRF para terminar llamando al mismo endpoint, y sobre todo abren un lugar cómodo donde la lógica de negocio se acumula sin que se note, que es lo que D34 prohíbe — si algo parece necesitarlas, casi siempre es un caso de uso que le falta al backend. El reenvío manual de la cookie en los Server Components queda en un solo archivo porque es el error clásico de Next con sesiones |
| D79 | **El sistema de diseño** (`docs/product/DESIGN_SYSTEM.md`), que baja D71 a tokens y componentes. **Paleta de dos neutros cálidos + un acento (ámbar de sala) + un peligro, con el contraste calculado y no estimado** (todos los pares de texto ≥4,5:1 y los no textuales ≥3:1 en los **dos** temas, tabla en el documento); el rojo queda reservado para lo destructivo, así que el acento no puede ser el rojo telón. **Tipografía del sistema, cero webfonts**: serif del sistema para títulos —que es la mitad de la identidad propia y cuesta cero bytes— y sans para el resto. **Escala tipográfica, ritmo de espaciado y breakpoints son los de Tailwind restringidos**, no redefinidos. **Una sola sombra en todo el sistema** (lo que flota); todo lo demás se separa con borde de 1 px, porque en tema oscuro una sombra no existe. **Hay tema oscuro y lo elige el sistema operativo (`prefers-color-scheme`), sin interruptor**: es un bloque `@media` con quince valores, cero JavaScript y cero estado, y ningún componente escribe `dark:` porque todos usan tokens de rol. **Los componentes de `components/ui/` son 10 y no ~8**: "botón seguir" se absorbe en `Boton` como variante alternante —seguir y like son el mismo control con la convención de tres estados de D67/D68—, entra `Afiche` (la imagen que puede no estar es el problema central de D71 y su respaldo tiene que ser idéntico en las tres superficies) y entra `Aviso` (el mensaje general de formulario que exigen el `409` sin `errores` del alta y el `400` sin `errores` del admin, más el `global: true` del feed: hay contenido, así que no es un estado vacío). **Los tres huecos de D71 quedan diseñados**: la ficha sin afiche **cambia de forma en vez de dejar un hueco** —una columna con el título grande, y la placa tipográfica sólo donde hay una celda de tamaño fijo que llenar (grilla, miniatura, `og:image`)—; el camino del catálogo cerrado lleva lo tipeado adentro del botón ("Sugerir «hamllet»") y termina en una **pantalla de acuse que dice que no va a haber aviso** (MD-3), no en un cartel que se va; y "en cartel" son dos secciones y una grilla **sin filtros ni calendario** (X4/P6), con una prueba de aceptación explícita: una grilla con la mitad de las celdas sin afiche tiene que leerse como una decisión. **Se decide también cómo se dibuja cada nulo que la API devuelve** (`autor`, `enCatalogo: false`, reseña sin texto, `rating`, `promedio`, `loSigo`/`leDiLike`/`vecesQueLaVi`, `SIN_FECHA`, las tres formas de la fila de la cola de reportes, el feed vacío en sus dos variantes): la regla es que **un dato que falta no deja un hueco, cambia la forma** — nada de cajas grises, guiones ni "sin datos". **Y quedan fijadas las dimensiones en las que se muestra un afiche** (ficha `contain` sin recortar, grilla y miniatura 2:3 recortadas por CSS ancladas arriba, `og:image` 1200×630), **que es el insumo que P16 estaba esperando: el lado mayor que se llega a mostrar es 1200 px y una caja de origen de 1200×1600 sin recortar cubre todo. P16 no se cierra acá** | Los contrastes están calculados con la fórmula de WCAG 2.1 y no elegidos a ojo porque el producto se lee en un celular a las 23:30 (Flujo 3, P8) y porque "se ve bien en mi monitor" es la forma habitual de descubrir el problema recién con usuarios. El tema oscuro entra por lo mismo: es el único escenario de uso que el producto se tomó el trabajo de escribir en tres documentos, y con tokens semánticos cuesta quince valores. Se descartó **sólo tema claro** (lo más barato, pero agregarlo después es más caro: para entonces hay 13 pantallas con colores decididos a mano) y se descartó el **interruptor manual**, que es la parte interesante: persistirlo en el servidor pide una pantalla de ajustes que **USER_FLOWS descartó explícitamente** (hueco 3: no hay nada que configurar, D21 + MD-4), y persistirlo en el cliente pide leer `localStorage` antes de pintar para evitar el destello, que con SSR (ADR-003) es justo el patrón que produce desajustes de hidratación — todo eso para reemplazar una preferencia que el usuario ya expresó una vez en su sistema operativo. Se descartó una **webfont** vía `next/font` (que no sería dependencia nueva, viene con Next): son decenas de KB en la primera visita del que llega por WhatsApp con datos móviles y, sobre todo, identidad prestada mientras **P2 sigue abierto** — el sistema está escrito para funcionar sin nombre ni logo, con el hueco del logotipo reservado y vacío en la placa OG. Es además la decisión más barata de revertir: es un token. Se descartó el **color determinístico por username** en el avatar (doce valores más que verificar en dos temas, con riesgo de combinaciones que no cumplan contraste, para duplicar una información que ya está al lado: el nombre). Se descartó **colorear el puntaje según el valor** —rojo el 3, verde el 9—: es una opinión, no un indicador, y teñirla es que el producto opine sobre la opinión. Se descartó el **`toast`**: se va solo, es hostil para quien lee despacio, no deja rastro del error y pide un sistema de notificaciones flotantes que nadie pidió. La ficha que **cambia de forma** en vez de mostrar una placa es la parte que más se discutió y es lo que separa "una imagen que no cargó" de "esta ficha se ve así": la placa se queda donde hay una celda que llenar sí o sí, y la ficha, que es la única pantalla que puede reacomodarse, se reacomoda. Que la grilla recorte por CSS y el archivo se guarde entero sale de que los afiches del teatro independiente de CABA no son un formato —verticales de programa de mano, cuadrados de Instagram, flyers apaisados— y suelen tener el título impreso adentro: recortar al subir lo destruiría para siempre para ganar algo que el navegador hace gratis. Queda anotado y **no cerrado acá** con qué se genera la placa OG: `next/og` viene con Next pero necesita un archivo de fuente embebido, y esto acaba de decidir que la web no carga ninguna — se resuelve al construir la ficha, no bloquea nada y no es P16 |
| D80 | **Las 13 pantallas, una por una** (`docs/product/SCREEN_SPECS.md`), que **cierra el paso 0 de la Fase 4**: qué llama cada una y por cuál de los dos clientes, cómo se compone en el celular, qué isla es cliente, y **los cuatro estados obligatorios —cargando, vacío, error y sin sesión— sin ninguno en blanco**. Lo que hizo falta decidir para escribirlas: **el gesto de registro es una hoja `<dialog>` y no una ruta**, abierta desde tres lugares —el botón persistente, el CTA de la ficha y "registrar de nuevo"—, con `pushState`/`popstate` para que el botón "atrás" del celular la cierre en vez de sacar al usuario de la pantalla; **el botón persistente de D71 cambia de identidad según la sesión** —"Registrar" con cuenta, **"Creá tu diario" sin ella**—, así que nunca abre un formulario que va a rebotar en un `401` y de paso es el CTA de adquisición que el Flujo 1 pedía en las cinco pantallas públicas; **en el autocompletado, "no está en el catálogo → sugerirla" es la última opción SIEMPRE, con resultados o sin ellos**; **el gesto y la sugerencia guardan su borrador en `sessionStorage`** y lo restauran al volver del login; **la ficha degrada por bloque** —si falla `opiniones` pero la ficha vino, la ficha se muestra igual y el bloque lleva su propio error—; **el sentinela del scroll infinito es un botón "Cargar más" de verdad** que el `IntersectionObserver` toca solo; **la confirmación de HU-08 es una pantalla de acuse y no un cartel que se va**; **el cambio de estado del admin va sin confirmación y fusionar con una que dice cuántos registros se mudan**; y **la sugerencia no sale de la cola al abrir el formulario, sino recién al guardar la ficha** (D69 llevado a pantalla). Más la matriz de cobertura de las 22 historias contra las 13 pantallas, que verifica en los dos sentidos | Escribir esto antes de la primera pantalla es la misma regla que hizo rápida la Fase 1 y que ya justificaron D78 y D79: cuando aparece algo imprevisto no hay que decidir dónde ponerlo. Los cuatro estados obligatorios están porque USER_FLOWS dice literalmente que ahí es donde los MVPs de una persona quedan flojos, y "cargando/vacío/error" es lo que se posterga cuando la pantalla feliz ya anda. **Se descartó la ruta `/registrar`**: el árbol de D78 no la tiene y agregarla obligaría a recargar la pantalla de atrás al volver, justo en los dos casos donde el contexto importa (registrar desde la ficha de la obra que se está mirando); lo único que la ruta daba gratis —el botón "atrás"— lo da `<dialog>` con dos líneas de historial. **Se descartó esperar a que el autocompletado vuelva vacío para ofrecer sugerir**, que era lo natural: con `pg_trgm` (D65) casi siempre vuelve *algo*, así que el caso real no es "no hay nada" sino "ninguno de estos es el mío", y esperar la lista vacía deja al usuario intensivo con historial —la primera impresión que R2 dice que no hay que arruinar— sin salida visible. **Se descartaron los CTA extra de registro** en las pantallas públicas (banner al final del scroll, interstitial): el visitante que sólo mira es la mitad del Flujo 1 y molestarlo es perderlo; el botón persistente ya está fijo abajo. **Se descartó una librería de gráficos** para las stats de HU-13: son barras de ancho porcentual con un `div` (D51/D73). **Se descartó simular paginado en el perfil**, que el contrato no tiene (hueco 4 de `API.md`). **Se descartó pedir confirmación al cerrar una producción desde el listado**: es reversible en otro clic y el barrido semanal de D37 vive de que eso sea un solo toque — la confirmación se reserva para lo irreversible (borrar, fusionar). El borrador en `sessionStorage` **no contradice lo que D79 descartó para el tema oscuro**: aquello necesitaba leerse antes del primer pintado para evitar un destello, que con SSR produce desajustes de hidratación; esto se lee después de montar y su peor caso es un campo que se completa 50 ms tarde. Y `?volver=` acepta **sólo rutas relativas del propio sitio**, porque aceptar una absoluta es un redirect abierto |
| D81 | **El armazón pasa de tres piezas a cuatro** (`product/SCREEN_SPECS.md` v1.1): entran un **menú principal** y una **barra de destinos fija abajo**, y la barra va **debajo del botón persistente**, que **no cambia** —sigue siendo un `Boton` primario de 44 px, con texto y con sus dos identidades, "Registrar" con sesión y "Creá tu diario" sin ella (D80)—, que es lo que mantiene cerrado el hueco 2 de USER_FLOWS y cumplida D71. **El menú principal** lo abre un botón en la cabecera, a la izquierda, con **ícono hamburguesa + la palabra "Menú" escrita**, y es un panel a pantalla completa en **`<dialog>` nativo** con las reglas de la hoja del gesto (captura de foco, `Esc` y clic afuera cierran, `pushState`/`popstate` para que "atrás" cierre en vez de sacar al usuario de la pantalla, D73/D80). Su contenido con sesión va **agrupado por categoría** —**Vos**: mi diario, sugerir una obra, salir · **Descubrir**: en cartel, buscar · **El proyecto**: código (AGPL, D46) · **Panel**, sólo si `rol === "ADMIN"`: sugerencias, reportes, producciones, salas, personas— y **sin sesión son dos ítems y ninguna categoría, "Entrar" y "Código"**: "crear cuenta" no va, porque ya es el botón persistente y repetirlo sería el segundo CTA de adquisición que el criterio transversal de las pantallas 1, 3, 4, 6 y 8 prohíbe. **La sección del panel no lleva contadores** ("7 sugerencias", "2 reportes"): `API.md` no los devuelve, y sacarlos pidiendo las dos colas para contar filas es lógica de negocio en el cliente (D34); si se quieren, son trabajo de backend. **La barra de destinos** son **4 celdas con sesión —Feed · En cartel · Buscar · Mi diario— y 3 sin ella —Inicio · En cartel · Buscar—**: "Mi diario" no existe sin sesión porque no hay diario que mostrar, y "Feed" se llama "Inicio" porque sin cookie esa ruta es la pantalla 1 y no la 2. Medidas: celdas de **82 px** en 360 (328 ÷ 4), objetivos de **48 px**, etiquetas en **`text-xs`** —el piso de la escala, y no baja de ahí—, `border-top` de 1 px en `borde` separando el botón de la barra, `pt-2 px-4 pb-3` + `pb-[env(safe-area-inset-bottom)]`, y **el destino activo marcado con regla de 2 px en `acento` + `font-medium` + texto en `acento-tinta`: nunca sólo con color**. **En ≥`md` no se dibuja ninguna de las dos**: la cabecera ancha ya tiene el buscador desplegado, el menú de cuenta y el botón de registrar, y ahí viven también los destinos. **Ninguno de los dos sube a `components/ui/`** —la regla de pertenencia pide una segunda pantalla que los repita y acá los usa el layout una sola vez para todas—: van en **`components/layout/`** y **el listado de diez componentes de `ui/` no cambia**. **La lista de íconos sube de seis a diez** (`DESIGN_SYSTEM.md` v1.1): entran **hamburguesa, casa y cartel** por esta decisión, y **tres puntos se regulariza** —ya lo pedían las pantallas 2 y 3 y nunca había entrado en la lista de seis, así que es una corrección y no un agregado—; "Buscar" usa la lupa que ya estaba y **"Mi diario" no estrena ícono: usa el monograma de `Usuario`**. Siguen siendo SVG en línea escritos a mano, sin librería de íconos (sería la dependencia que D73 dejó afuera) y ninguno viaja solo sin etiqueta accesible. **No aparece ninguna pantalla nueva**: el alcance sigue congelado (D27), el inventario de USER_FLOWS sigue en 13 y el hueco 3 sigue cerrado (no hay pantalla de ajustes) | **El hueco que cierra era real y no cosmético**: el armazón sólo tenía menú de cuenta en ≥`md`, así que **en el celular no había camino al diario propio, ni a salir, ni al panel** — y el diario propio es el producto (D2), sin entrada visible justo en el dispositivo de referencia del sistema (360 px, el celular de las 23:30, P8/Flujo 3). Se descartaron, de menos a más ambiciosa: **sólo extender el menú de cuenta al celular**, la mínima, que resolvía el hueco con cero íconos nuevos y sin costo de espacio, pero deja los destinos frecuentes a dos toques —abrir y elegir— en la aplicación que se usa parada en la vereda; **sólo el menú con categorías, sin barra**, que sobra taxonomía para seis destinos: agrupar en tres títulos lo que entra en una fila es ceremonia; **la barra inferior con el botón de registrar como círculo con un `+` al centro**, que es **el descarte importante** y la razón por la que el botón quedó por encima y no adentro de la barra: convertía el CTA en un símbolo, y el armazón lo necesita **como texto que cambia según la sesión** —un `+` no puede decir "Creá tu diario"—, así que se perdía justo lo que D80 había resuelto; **la misma mezcla sin ítems duplicados**, más prolija, que pierde el mapa completo que hace descubrible el producto para el que recién llega; y **la barra sin íconos, sólo texto**, que costaba un ícono nuevo en vez de tres y era la más coherente con el eje tipográfico de D71 — se descartó porque en un uso diario el reconocimiento del ícono bajo el pulgar pesa más que la coherencia del eje, que la barra tampoco define. **Los ítems duplicados son la decisión, no un descuido**: Mi diario, En cartel y Buscar están en la barra y también en el menú porque **la barra es para el pulgar dentro de una sesión y el menú es el mapa completo del producto** — el que recién llega abre el menú y ve todo, el intensivo no lo abre nunca y llega a lo suyo en un toque; es la misma redundancia deliberada de todos los menús de este tipo. **Costos asumidos, los cuatro escritos:** los tres ítems duplicados; los tres íconos nuevos más el cuarto que se regulariza; **~50 px de contenido**, porque el cromo fijo pasa de ~128 px a ~178 px en un 360×640 y quedan ~460 px; y la **menor descubribilidad de los tres ítems que sólo viven en el menú** —Sugerir, Salir, Código—, porque el menú enseña rápido que no hace falta abrirlo: la contramedida es que a "Sugerir" se llega igual por su camino natural, la última opción del autocompletado del gesto, que está siempre (D7/D24), así que ninguno de los tres es parte del camino feliz. **Queda por verificar, y es concreto**: la ficha (pantalla 3) con el afiche a 60 vh contra el cromo nuevo — con esa altura el título aterriza justo en el pliegue, se mide al escribir la ficha y lo que se ajusta si no entra es el máximo del afiche, no la barra (48 px y `text-xs` son pisos). Documentos actualizados por la regla de mantenimiento: `SCREEN_SPECS.md` (v1.1, el armazón y el costo de espacio anotado), `DESIGN_SYSTEM.md` (v1.1, íconos y medidas de la barra), `FRONTEND_ARCHITECTURE.md` (`components/layout/`) y `USER_FLOWS.md` (la nota de que tres caminos son del armazón y no de una pantalla). `API.md` **no se toca: esto no cambia el contrato HTTP** |
| D82 | **Cada archivo de `lib/api/` se parte en dos por lado de ejecución: `<modulo>.servidor.ts` y `<modulo>.cliente.ts`.** La regla de D78 no cambia —**un módulo del backend, un cliente del frontend**, y `lib/api/` sigue siendo el único lugar con `fetch`—: lo que se parte es el archivo, no el módulo. El motivo no es de gusto: **un archivo con las dos mitades no compila.** `server.ts` importa `next/headers` para reenviar la cookie, y en cuanto un componente cliente importa una función de ese mismo archivo —`BotonSalir` importando `logout`— Next mete `next/headers` en el bundle del navegador y el build falla con "You're importing a module that depends on `next/headers`". Casi todos los módulos tienen las dos mitades (Identidad: `yo` en el servidor, `login`/`registro`/`logout` en el navegador; Catálogo: las fichas por SSR y la búsqueda del autocompletado por el cliente; Diario: perfil y opiniones contra crear/editar/borrar), así que el par es la forma normal y no la excepción. Un módulo con un solo lado tiene un solo archivo: el panel es todo cliente, así que va a ser `admin.cliente.ts` y nada más. **`server.ts` y `client.ts` no se tocan**: ya eran los dos lados, y el sufijo es el mismo par bajado un nivel. Se agregan además dos piezas que la estructura de D78 no nombraba y que aparecieron al escribir el armazón: **`lib/api/errores.ts`**, que es donde viven el aplanado de las tres familias de `API.md` y la tabla de mensajes por código, compartido por los dos clientes —tenerlo en cualquiera de ellos lo haría inalcanzable para el otro, que es el mismo problema de arriba—; y **`components/ui/usarDialogo.ts`**, el comportamiento común de las tres cosas que se superponen (menú principal, hoja del gesto, `Confirmacion`) | Sale de escribir el paso 1 de la Fase 4, no de una discusión: el build lo dijo. Se descartaron dos salidas. **Importar `next/headers` de forma diferida** dentro de la función (`await import(...)`) mantiene un archivo por módulo, pero deja la trampa armada —el día que alguien llame esa función desde el cliente falla en tiempo de ejecución y no al compilar— y cambia un error de build claro por uno de producción; el valor de la estructura de D78 es avisar temprano, no esconder. **Dejar que cada componente cliente llame a `apiMutar` con la URL a mano** ahorra el archivo y rompe la regla 2 de D78: el endpoint se escribe en el componente y `lib/api/` deja de ser el único lugar que sabe de la API. El sufijo, en cambio, **hace la regla imposible de romper por accidente**: el nombre del archivo dice desde dónde se puede importar, y el par `servidor`/`cliente` ya es el vocabulario del documento (`apiPublic`/`apiSession` contra el navegador). ⚠️ **Y con el paso 1 se cierra la comprobación que D78 había dejado pendiente**: `curl -si localhost:8080/api/auth/yo` contra el backend corriendo responde `HTTP/1.1 401` **con `Set-Cookie: XSRF-TOKEN`**, así que la semilla perezosa del token es la que estaba escrita y **no hace falta el reemplazo por `GET /api/en-cartel`**. Verificado también a través del rewrite de Next (`localhost:3000/api/auth/yo`), que es el camino real del navegador en desarrollo |

| D83 | **Tres cosas del esqueleto que salieron de auditarlo, y las tres son estructurales.** (1) **El armazón no vive en el layout raíz sino en un grupo de rutas `app/(sitio)/`.** `SCREEN_SPECS.md` dice que las cuatro piezas están en todas las pantallas **salvo el panel** (D81), y en el App Router **los layouts se anidan**: un `app/admin/layout.tsx` no reemplaza al de arriba, se mete adentro. Con el armazón en la raíz, el panel lo llevaría puesto para siempre y sacárselo obligaría a mover el árbol de rutas entero. Entonces la raíz es `<html>`/`<body>` y los tokens, el armazón baja a `(sitio)` y `admin/` cuelga por afuera con el suyo; **los paréntesis no cambian ninguna URL**. ⏳ Consecuencia anotada, **y no es opcional**: una URL que no matchea nada usa el `not-found.tsx` de la **raíz**, que se dibuja con el layout mínimo, así que **la pantalla 13 tiene que componer el armazón a mano ahí** —pedir `yo()` y envolver en `Cabecera`, `Pie` y `BloqueInferior`—. Dejar el 404 global sin cabecera ni pie **no es una alternativa aceptable**: `SCREEN_SPECS.md` pide el armazón completo en las dos pantallas de error porque es la única salida que le queda a quien llegó por un link roto. El `notFound()` de una ficha inexistente no tiene el problema: cae en el `not-found.tsx` de `(sitio)`. (2) **Una página conoce la sesión volviendo a llamar a `yo()`, que está memoizada con `cache()` de React.** Next no pasa datos de un layout a sus hijos, y subir la sesión a un contexto de cliente convertiría el armazón en cliente y rompería el SSR de ADR-003. `cache()` memoiza **por pedido**: el layout la pide para el armazón, la ficha y el perfil la piden para **elegir entre `apiPublic` y `apiSession`** (las dos ⚖️ de `SCREEN_SPECS.md`), y la red se toca una vez. No es caché entre pedidos —`apiSession` sigue siendo `no-store`— y muere con el render. (3) **Si `yo()` falla, se dibuja el armazón del visitante y no una pantalla de error.** Sólo el `401` significa "anónimo", pero **el resto también degrada, y conviene decir el alcance real porque es más ancho de lo que suena**: no sólo el `5xx` y la red caída, sino **cualquier excepción que no sea control de Next ni el `401`** —un `403` inesperado, un cuerpo que no respeta el contrato, un bug de la propia función—. Es a propósito: la regla no es "qué error fue", es **"el armazón nunca tira la pantalla abajo"**. Porque **un error tirado desde un layout no lo atrapa su `error.tsx`** —lo agarra el límite del segmento de arriba—, así que propagarlo serviría la pantalla global de error en **todas** las URLs, incluidas las públicas que Next todavía puede servir de su caché de datos | Las tres salen de una auditoría externa del paso 1, y **las tres eran defectos reales del código escrito**, no diferencias de gusto. La (1) es la más cara si se posterga: es la única de todo el esqueleto que después obliga a mover archivos en vez de editar uno. La (3) es donde hubo desacuerdo y conviene dejar el razonamiento: la objeción —que un backend caído le muestre "Creá tu diario" a alguien con sesión es un estado falso— **es correcta**, y el `catch` que devolvía `null` ante cualquier error era una decisión invisible, que es el problema de fondo. Lo que no se acepta es el remedio de propagar: `SCREEN_SPECS.md` es explícito en que **la degradación es por bloque** —"si falla `opiniones` pero la ficha vino, la ficha se muestra igual"— y en que "la mitad que sostiene el Flujo 1 —título, afiche, sinopsis, el preview de WhatsApp— es la primera"; hacer que un parpadeo de `/auth/yo` tire abajo la ficha compartida por WhatsApp invierte esa prioridad justo en la pantalla que ADR-003 existe para sostener. Así que se queda el comportamiento y **se paga el precio de escribirlo**: la decisión queda acá, el código dice por qué en vez de tragarse el error en silencio, y el servidor lo registra. **El segundo costo, que la primera redacción de esta entrada no decía y hay que decir**: con el `catch` así de ancho, **un bug de `yo()` se ve como "no hay sesión" y no como un error**, así que el `console.error` del servidor no es decorativo: es el único lugar donde ese caso aparece. **Queda pendiente y anotado**: cuando exista `Aviso` (paso 5), decidir si "no pudimos verificar tu sesión" merece una banda — hoy no hay con qué dibujarla. Sale además una corrección de `DESIGN_SYSTEM.md`: `Usuario` variante `cabecera` decía `text-lg` y va en `text-3xl`/`sm:text-4xl`, porque la usa una sola pantalla —el perfil— donde el username **es el título**, que es el escalón que la propia tabla de la escala le asigna |

| D84 | **Los botones van en infinitivo, y se termina la excepción del panel admin** — cierra **P17/MD-6**. Gana `SCREEN_SPECS.md`, que ya nombraba las etiquetas así ("Registrar", "Publicar", "Registrar que la vi", "Registrar de nuevo", "Enviar", "Guardar", "Borrar registro"), y **se corrige la regla de voz de `DESIGN_SYSTEM.md`**, que decía lo contrario: imperativo voseante con el panel como única excepción. Consecuencia concreta y única sobre lo ya escrito: el botón persistente sin sesión pasa de **"Creá tu diario" a "Crear tu diario"**, lo que **supersede la etiqueta que fijaron D80 y D81** —el resto de esas dos decisiones no se toca—. **El voseo no se va del producto**: sigue en títulos, estados vacíos, avisos y errores —"Todavía no registraste nada", "Revisá el email", "Seguí gente para armar tu feed"—, que es donde la voz se escucha. Un botón es una etiqueta, no una frase | Lo pedía el ROADMAP para este paso ("antes de escribir el primer `Boton`") y ya estaba mordiendo: el armazón terminó con **"Registrar" al lado de "Creá tu diario"**, las dos sacadas al pie de la letra de D80/D81, así que la incoherencia estaba en los documentos y no en el código. A favor del infinitivo: **es lo que ya estaba escrito en las 13 pantallas**, así que cerrar al revés obligaba a reescribir `SCREEN_SPECS.md` entero para ganar nada verificable; **es más corto**, que en una celda de 82 px de la barra de destinos no es un detalle; y **borra una excepción** —el panel dejaba de hablar distinto del resto— en un producto que tiene un solo redactor. Lo que se pierde y se anota: el imperativo voseante es más cálido y más rioplatense, y ese era el argumento fuerte del otro lado; se acepta porque **la calidez del producto no vive en los botones**, vive en las frases que los rodean, y esas no cambian. Decidido por el fundador |

| D85 | **La placa de `og:image` se genera con `next/og` y una serif embebida, y vive en una ruta propia `/og/{tipo}/{id}`** — cierra lo que `DESIGN_SYSTEM.md` (D79) y `SCREEN_SPECS.md` (D80) habían dejado anotado sin resolver: *con qué* se dibuja esa imagen. Tres partes. (1) **La fuente va embebida y no puede no estarlo**: `next/og` compone con satori, que **no lee fuentes del sistema** —sólo dibuja con los bytes que se le pasan— y lo único que trae por defecto es una sans. Sin archivo, la placa sale sin la serif, que es la mitad de la identidad propia de D71 y justo en la única imagen que ve el que todavía no entró. Entra entonces **`frontend/assets/NotoSerif-Regular-subset.ttf`**: Noto Serif (OFL 1.1) **subseteada a latín** con `fontTools` — **30 KB**, con su licencia y el comando exacto al lado, en `frontend/assets/OFL-1.1-NotoSerif.txt`. **No es una webfont y no contradice a D79**: el navegador no la descarga nunca, se usa **sólo en el servidor** para generar esta imagen, y el sitio sigue con las dos familias del sistema. **Tampoco es una dependencia** en el sentido de D51: no hay paquete nuevo en `package.json` —`next/og` viene con Next—, es un archivo de datos, como lo sería un `.svg`. (2) **Una ruta propia y no el `opengraph-image.tsx` convencional de Next**, que era el camino obvio: el archivo convencional **gana sobre lo que devuelva `generateMetadata`**, y la regla de D79 es que **cuando la ficha tenga afiche el `og:image` es el afiche tal cual** (ya es una URL absoluta, estática e inmutable: no hay nada que generar). Con el archivo esa elección no se puede escribir; con una ruta que se nombra desde `generateMetadata`, queda en una línea legible al lado del `??`. Es **una sola ruta para los tres tipos** —obra, artista y sala— porque la placa es un diseño y tres copias se separan en la segunda corrección; `usuario` entra con la pantalla 8. (3) **`metadataBase`**, que sale de una variable de entorno (`SITIO_URL`, con el `localhost:3000` de desarrollo por defecto) y **es la pieza que hace absolutos el `og:image` y el `canonical`**: sin ella Next los emite relativos y **el preview de WhatsApp se queda sin imagen**, porque el servidor que lo arma no está en nuestro origen. Los seis campos de las cuatro pantallas compartibles viven en **`lib/metadatos.ts`**, una sola vez | Sale de escribir el paso 2 de la Fase 4, que es donde los dos documentos habían dejado la pregunta. Se descartaron las otras dos salidas y conviene decir por qué, porque la barata era tentadora: **usar la sans que ya trae `next/og`** no cuesta ni un byte nuevo, y se descarta porque el `og:image` es hoy **la única imagen del producto** —no hay afiches hasta que cierre P16— y renunciar a la serif ahí es renunciar a la identidad entera en el único lugar donde la ve alguien que no entró todavía. **No generar imagen hasta que existan los afiches** es lo que `SCREEN_SPECS.md` llama degradado y no roto, y es cierto para una ficha suelta; aplicado hoy sería **el catálogo entero sin preview**, con el test literal de HU-04 vacío hasta la Fase 5. El costo asumido del subconjunto: cubre latín básico, Latin-1 y Latin Extended-A —o sea castellano y los nombres europeos que aparecen en un catálogo de CABA—, y **un título con un glifo fuera de ese rango saldría con un cuadrito**; el comando queda escrito para poder rehacerlo más ancho. ✅ **Verificado en pantalla, no supuesto**: la placa de una ficha corta y la de un título de 79 caracteres, las dos con la sala y el estado, y los `<meta>` de la ficha con la URL absoluta. La imagen sale `1200×630` PNG, siempre oscura en los dos temas (D79), con los 60 px de margen de seguridad y el hueco del logotipo de P2 vacío |

| D86 | **Dos correcciones de la celda de la grilla, las dos salidas de mirarla renderizada y no de leerla.** (1) **El título aparece una sola vez por celda, y dónde depende de si hay afiche**: con afiche va en el pie tipográfico, debajo de la imagen; **sin afiche ya está adentro de la placa** —en la serif y en grande— y el pie se queda sólo con la sala. `SCREEN_SPECS.md` decía "afiche **o placa** + título + sala", y **al dibujarlo el título se ve dos veces en 164 px de ancho**: se lee como un bug, que es exactamente lo que el criterio de aceptación de esta grilla no perdona (D79: "una grilla con la mitad de las celdas sin afiche tiene que leerse como una decisión"). (2) **La caja de la imagen crece dentro de la celda**: la grilla estira todas las celdas de una fila a la altura de la más alta —que es lo que pide "altura pareja"—, y sin eso la celda con menos texto rellenaba el sobrante con **un rectángulo vacío debajo del pie**, que es el hueco que la regla 2 de D79 prohíbe. Estirando la placa, la celda sigue siendo una placa. Se agrega además un `sinSala` que usa **la pantalla de sala**: ahí la sala es el `h1` y cada celda la estaba repitiendo. **Nada de esto cambia el sistema**: los tokens, los tamaños y las tres columnas siguen igual. ⚠️ **Y trae una consecuencia de accesibilidad que hay que escribir, porque la primera versión la rompió**: `SCREEN_SPECS.md` dice que la placa es decorativa y va con `aria-hidden` "porque el título ya está al lado como texto" — **y con este cambio dejó de estar al lado: pasó a estar adentro**. Con la placa oculta, el link de una celda sin afiche **se queda sin nombre accesible**, y en la pantalla de sala —donde tampoco hay sala ni chip— literalmente sin nada que anunciar. Así que **el `aria-hidden` queda sólo en la miniatura**, que es donde la frase original sigue siendo cierta. La regla que manda es la razón, no la frase: **se oculta lo que se repite al lado, nunca lo único que nombra a un control** | La entrada existe porque la regla de mantenimiento pide que el documento se corrija cuando la realidad lo contradice, y acá la realidad fue una captura: **las dos cosas eran invisibles en el texto de la especificación y evidentes en la pantalla**. Vale como precedente para el resto de la fase: la placa de D71 es el único componente del sistema cuyo criterio de aceptación es visual —"que se lea como una decisión"—, así que se mira antes de darla por escrita. `DESIGN_SYSTEM.md` y `SCREEN_SPECS.md` actualizados con la regla del título |

| D87 | **El manejo global de errores: la API pasa de tres formas de error a una sola.** Entra un `@ControllerAdvice` en la capa de aplicación (`ErroresDeApi`) y, porque un advice no puede tocar lo que pasa antes del despacho, entran también los dos manejadores de la cadena de filtros (`RespuestasDeSeguridad`, `SecurityConfig`). Con esto **todo error de la API es un `ProblemDetail` con `detail` en castellano**, y el mapa `errores` por campo lo devuelven **todos** los formularios y no cuatro: el hueco concreto que esto vino a tapar es que el panel admin validaba igual que el resto pero su `400` no decía qué campo estaba mal, así que la pantalla solo podía decir "revisá el formulario". Cuatro cosas más quedan cubiertas: el `401`/`403` de Spring Security y el `403` de CSRF —con mensajes distintos, porque uno no se arregla reintentando y el otro sí—, los errores de Spring MVC (`404` de ruta inexistente, JSON roto, `405`, `415`) que salían con el cuerpo genérico de Boot y en inglés cuando traían mensaje, y **el fallo inesperado, que ahora es un `500` con forma y sin tripas, con el error completo en el log del servidor**. **Los errores de dominio NO se mueven**: siguen declarados en su controlador, y un `@ExceptionHandler` local le gana al global | Cierra el hueco 2 de `API.md`, que era trabajo de backend de esta misma fase (ROADMAP, paso 3). Que los errores de dominio se queden donde están no es media reforma: **la mitad de esas excepciones son internas de su módulo y la capa de aplicación no las puede ni nombrar** (ADR-001, `ModulithArchitectureTest`), y la otra mitad no tiene un estado HTTP fijo — `DataIntegrityViolationException` es un `409` al borrar una sala referenciada y un `204` al dar dos veces el mismo like—. Lo que se unifica es la **forma**, no el significado: qué es cada error lo decide el endpoint. Que el handler local gane es además lo que mantiene en pie los mensajes que son una decisión, como el `401` del login, genérico a propósito para no revelar cuál de los dos campos falló (HU-02). **Tres costos, escritos.** (1) El `401` ahora **trae cuerpo**, y eso obligaba a comprobar algo que no era obvio: que escribirlo no se lleve puesta la cookie `XSRF-TOKEN` que ese mismo `401` siembra (D78/D82) — se fija el estado y se escribe, sin `sendError` ni `reset`, y hay un test que lo vigila, porque si se rompe, el visitante que llega por un link compartido no puede crearse una cuenta. (2) El manejador de último recurso **cambia lo que ve un test**: hasta acá una excepción inesperada salía por MockMvc como un throwable, y los dos tests de rollback —fusión (D63) y resolución de reportes (D70)— la atrapaban con `assertThatThrownBy`; ahora el cliente recibe un `500`, así que esos dos tests comprueban el estado y la excepción resuelta. No perdieron nada: lo que verifican —que no quede media operación hecha— es todo lo que sigue después. (3) Los mensajes por campo dejan de ser específicos del formulario ("Revisá los datos del registro") y pasan a uno solo, "Revisá los datos ingresados": el detalle vive en el mapa `errores`, que es lo que la pantalla dibuja. Se descartó **un advice que también mapeara los errores de dominio**: para eso habría que subir esas excepciones a la interfaz pública de cada módulo, que es exponer más superficie de la que hoy existe para ganar un archivo menos. Y se descartó **traducir cada error de Spring MVC con un método por excepción**: se traduce en los dos puntos por donde la clase base arma el cuerpo, así un error que hoy no ocurre entra ya traducido el día que ocurra |

| D88 | **Cierra P16: el afiche se procesa con TwelveMonkeys y se guarda en JPEG** — y eso **enmienda D77 en un punto**: la URL pública termina en `.jpg` y no en `.webp`. Entran **dos dependencias nuevas** (`com.twelvemonkeys.imageio:imageio-jpeg` e `imageio-webp`), excepción explícita a MODO ESENCIAL decidida y no adoptada, como se decidió Tailwind en D73. **Son lectores, no escritores**: la salida la escribe el JDK. El motivo de que no sea WebP es que **no existe un escritor de WebP en Java puro** —el JDK escribe JPEG y PNG y nada más—, así que WebP a la salida obligaba a un binario del sistema (`cwebp`) o a una librería con código nativo. **El resto de P16, que también se cierra acá**: se encaja en **1200×1600 sin recortar** (D79) y **nunca se agranda**; calidad **0,82**; la **orientación EXIF se aplica al subir** y el archivo se guarda sin metadatos; el alfa se aplana sobre blanco; y hay un **tope de píxeles decodificados** de 50 MP, configurable, que se comprueba **leyendo la cabecera antes de decodificar** | Lo que la dependencia compra, y es lo que inclinó la decisión: acepta los **tres** formatos de entrada que `API.md` promete —el JDK no lee WebP, y hoy cualquier imagen bajada de una web moderna llega en WebP— y **no se planta con los JPEG CMYK ni con los progresivos**, que es exactamente lo que sale de una imprenta y por lo tanto lo que llega en un afiche de teatro. Se descartaron las otras dos. **Cero dependencias, sólo `ImageIO` del JDK**: la más fiel a D51 y la que se pagaba en las horas que son la restricción más dura (D37) —rechazar WebP de entrada, romperse con CMYK, y la rotación EXIF escrita a mano leyendo el APP1—. **`cwebp` del sistema**: cumplía el contrato de D77 tal cual y con ~30% menos bytes, pero mete una dependencia de sistema, obliga a tocar el Dockerfile antes de la Fase 5 y pone un `ProcessBuilder` a comerse un archivo que sube el admin. **El costo asumido**: JPEG pesa ~25-30% más que WebP a igual calidad, y eso se paga justo en el preview de WhatsApp que ADR-003 vino a comprar. Se acepta porque **es la parte reversible**: gracias al versionado de D77, el día que haya un escritor de WebP en Java puro el cambio es el codificador y una constante con la extensión, y la migración es volver a subir los ~50 afiches (D38) — no reescribir URLs, porque ninguna URL vieja se reutiliza jamás. **Lo que NO cambia de D77**: el contador monótono, `afiche_actual` separado del contador, el orden de las cuatro operaciones, el borrado del archivo viejo después del commit, la idempotencia del `DELETE` y el `immutable` de un año, que nunca dependió del formato sino de que la URL no se reutilice |

| D89 | **Lo que encontró la auditoría del paso 3, y las tres correcciones que hizo falta. Las tres eran defectos reales, no diferencias de gusto.** (1) **La orientación EXIF se lee ahora en los tres formatos que se aceptan, y no sólo en JPEG** — corrige D88, que prometía aplicarla sin distinguir formato mientras el código devolvía "derecha" para PNG y WebP, con un comentario que además decía algo falso: que esos dos no pueden traer EXIF. **Sí pueden**: el PNG lo guarda en un bloque `eXIf` —parte de la especificación desde 2017— y el WebP en un bloque `EXIF` de su contenedor RIFF, algunos con el prefijo `Exif\0\0` del JPEG y otros sin él. Ninguno de los tres lectores la aplica solo. (2) **Los dos tests de solapamiento no probaban el bloqueo de fila que D77 exige**, y esto no es una sospecha: **sacándole el `@Lock` al repositorio, los doce tests de afiches seguían verdes**. Corrían los pasos uno detrás del otro en un solo hilo, así que probaban el orden y no la concurrencia; el único multihilo largaba con un latch pero no forzaba que las dos transacciones coincidieran adentro de la sección crítica. Entran **dos tests que fuerzan el solapamiento** con `TransactionTemplate` y `CountDownLatch` —uno tiene la fila tomada medio segundo, el otro tiene que esperar y ver lo que el primero publicó—, y **los dos fallan sin el `@Lock`**, que es la prueba que faltaba. Los secuenciales se quedan, renombrados a "intercaladas" y con la advertencia escrita de lo que no prueban. (3) **El movimiento atómico del archivo dejaba de serlo en silencio**: si el sistema de archivos no lo soportaba, se caía a un `move` común con un `warn` en el log. Ahora falla, porque D77 promete que nadie puede leer un afiche a medio escribir y un reemplazo no atómico es exactamente esa ventana. **Y una cuarta cosa que no es corrección sino cobertura**: entran tres archivos de prueba versionados —un WebP, un WebP con EXIF y un JPEG CMYK— con el comando que los genera al lado, como la fuente de D85, así que lo que justifica la dependencia de D88 pasa de "lo promete el `pom.xml`" a "lo prueba un test" | La auditoría fue externa y se pidió con la regla de siempre: el desacuerdo se sostiene con código o no vale. **Las tres primeras se aceptaron después de medirlas, no de leerlas** — la del bloqueo se comprobó sacando el `@Lock` y corriendo la suite, y la del EXIF sacando el soporte de PNG/WebP y viendo caer el test nuevo—. La (2) es la más incómoda y la que deja la lección: **un test de concurrencia que no falla cuando se rompe lo que dice cuidar no es un test de concurrencia**, y D77 lo había escrito con todas las letras ("deterministas y concurrentes... con la sincronización explícita"). La (3) es la regla general detrás: no prometer una garantía en el contrato y rebajarla en ejecución; si un entorno real no soporta el movimiento atómico, se enmienda el contrato, no el silencio. **Lo que queda sin cobertura y se anota en vez de esconderse**: el `413` del archivo que se pasa de 5 MB no tiene test — MockMvc no aplica los límites del contenedor, y probarlo pide Tomcat real más una sesión de admin sobre HTTP para llegar hasta el parseo, que es mucho andamio para una línea declarativa de configuración. **Y lo que falló del proceso, que también se anota, porque falló dos veces**: la regla de mantenimiento dice "en el mismo PR", y el paso 3 actualizó `API.md`, `FRONTEND_ARCHITECTURE.md`, el ROADMAP y el log, pero dejó desactualizados el README, `SCREEN_SPECS.md`, `DESIGN_SYSTEM.md`, los tipos del frontend y **el cuerpo de la propia D77**. La primera corrección los arregló **por búsqueda puntual**, y una segunda auditoría encontró que eso alcanzaba para las líneas buscadas y no para el documento: quedaban `⏳` en siete lugares de `SCREEN_SPECS.md`, tres afirmaciones de "P16 sigue abierto" en `DESIGN_SYSTEM.md` seis líneas debajo de decir que se cerró, el conteo viejo de tests en el ROADMAP y cuatro comentarios del frontend diciendo que el backend no manda lo que ya manda. **La lección, y es la que queda escrita para el resto de la fase: cuando una decisión cambia el estado de algo, el documento no se corrige buscando la frase que uno recuerda, se barre entero** — `grep` del símbolo (`⏳`), del identificador (`P16`, `D76`, `D77`) y de la afirmación ("el backend todavía no", "no existe hasta"), en `docs/` y en el frontend, y el barrido se muestra vacío antes de dar el paso por cerrado |
| D90 | **Lo que salió de escribir el paso 4 de la Fase 4 —las cuatro pantallas con formulario— y no estaba decidido. Seis cosas, y las cuatro últimas son defectos que el paso encontró, no gustos.** (1) **`Campo` y `CampoLargo` viven en `components/ui/` y el listado de diez de `DESIGN_SYSTEM.md` NO cambia.** Los diez son el vocabulario visual del producto —lo que se ve en una pantalla terminada—; esto es la plomería de un formulario: etiqueta, control, ayuda, y el error debajo del input con `aria-describedby` apuntando a los dos a la vez. Está en `ui/` por la **regla de pertenencia** —no sabe de ningún dominio y lo usan cuatro pantallas (alta, login, sugerir y el gesto), así que nacer en la carpeta de un dominio sería elegir uno al azar— y no por la lista. Es el mismo lugar y el mismo criterio con el que D82 puso ahí `usarDialogo.ts`, y con el que ahora entra `usarBorrador.ts`. (2) **`apiMutar` gana una opción para el `401`, y la usa una sola llamada: el login.** `POST /api/auth/login` responde `401` cuando las credenciales están mal, y ahí `401` **no significa "tu sesión venció"**: mandarlo al manejador global sería **recargar la pantalla de login desde la pantalla de login**, perdiendo lo tipeado y sin llegar a mostrar nunca el mensaje genérico que HU-02 pide. Es la tercera vez que la tabla de errores de D78 se cumple al pie —el código no alcanza, decide el contexto— y la primera en que eso necesita quedar escrito en el cliente. (3) **Los formularios llevan `method="post"` aunque el envío lo haga JavaScript.** Si el bundle todavía no hidrató —o falló—, el navegador envía el formulario **nativamente**, y un `form` sin método hace un `GET`: **la contraseña termina en la URL**, y de ahí en el historial, en el `Referer` y en los logs de cualquier proxy. Con `post` no viaja en la URL. **Lo encontró un navegador de verdad y no la lectura del código**, en el humo del paso. El buscador va al revés y a propósito —`action="/buscar"` con `method="get"`—, porque ahí el envío nativo es justo el que se quiere: sin JavaScript, `Enter` navega a `/buscar?q=…` y la pantalla se sirve desde el servidor con la búsqueda hecha. (4) **`Boton` fija `type="button"` por defecto**, al revés que el HTML: adentro de un formulario, un botón sin `type` lo envía, así que "Borrar registro" abría la confirmación **y** publicaba el registro. El que envía lo dice (`type="submit"`). (5) **`usarDialogo` gana `conHistorial: false`, para lo que se superpone a algo que ya empujó su entrada** — hoy, el `Confirmacion` de borrar adentro de la hoja del gesto. Con los dos empujando, cancelar la confirmación hacía `history.back()` y ese `popstate` cerraba también la hoja de abajo: **cancelar borraba lo tipeado**. Sin entrada propia, cancelar no toca el historial y "atrás" cierra las dos, que es lo que "atrás" quiere decir. (6) **La escala de puntaje es un control segmentado y no diez botones separados**, y eso **precisa el "≥32 px de ancho" de D80**: en un celular de 360 quedan **328 px** adentro de la hoja, y diez objetivos con separación no llegan a 32 px cada uno. Con los bordes compartidos dan **31,9 px**, que se lee como una escala y cumple de sobra el mínimo de 24 px de WCAG 2.5.8 AA — el 44 de alto no se toca | Ninguna de las seis cambia una decisión anterior: cuatro son defectos y dos son huecos que los documentos del paso 0 no podían tener, porque aparecen recién cuando hay un formulario escrito. La (1) es la que más se discutió, y el criterio que la resuelve es el que ya estaba: **la lista de diez es de vocabulario visual, no de archivos en la carpeta**; leerla como un inventario cerrado obligaría a repetir la misma etiqueta y el mismo `aria-describedby` en cuatro pantallas, que es exactamente lo que la regla de pertenencia existe para evitar. La (3) es la más incómoda y por eso queda escrita entera: **el formulario "funcionaba"** —con JavaScript, que es el caso normal—, y el agujero sólo aparece en la ventana entre que el HTML llega y el bundle hidrata, que es justo la ventana del que entra por un link compartido con datos móviles (Flujo 1). Se descartó **exigir JavaScript** con un `onSubmit` que ya está: no cierra la ventana, porque el manejador todavía no existe cuando el usuario aprieta. La (5) y la (4) son de la misma familia y vale decirlo: **los dos eran comportamientos por defecto del navegador que el componente no había elegido**, y las dos veces el default hacía algo que nadie quería. ⚠️ **Lo que este paso deja anotado y no resuelve**: editar y borrar (HU-11) están escritos y **no tienen punto de entrada** —los menús de tres puntos del diario y del feed son el paso 5—, así que la única prueba que corrieron es la del contrato; y **el `?volver=` del `401` de una acción protegida vuelve a la URL, no al lugar de la pantalla**: la hoja del gesto se reabre con su borrador porque deja una marca propia, pero eso es una pieza del gesto y no una regla general del armazón |
| D91 | **Lo que encontró la auditoría del paso 4 —y la re-auditoría de las correcciones—. Cuatro defectos reales, tres inconsistencias, un pendiente que se cierra y cuatro referencias mal atribuidas.** **(1) `olvidar()` no olvidaba, y el borrador se quedaba con la fecha congelada.** Publicar hace dos cosas seguidas —olvidar el borrador y vaciar el formulario— y el vaciado es un cambio de estado como cualquier otro: el efecto que persiste corría detrás y **volvía a guardar lo que se acababa de borrar**. **Medido en el navegador, no deducido**: después de publicar, `sessionStorage` tenía un borrador vacío con la fecha del día. Con una pestaña que sobrevive a la medianoche —la del celular, que es el dispositivo de referencia— el gesto siguiente ofrecía **la fecha de ayer con la misma cara con que ofrece la de hoy**, y eso registra la función equivocada en silencio, que es exactamente lo que MD-1 no permite. Dos arreglos, porque son dos problemas: `olvidar()` se saltea la escritura que le sigue, y **"hoy por defecto" se recalcula al abrir** —no al montar— cuando el formulario **está en blanco**, que es lo que separa un borrador del estado inicial. **(2) Un borrador viejo contaminaba una sugerencia nueva.** Al llegar desde la búsqueda con otro título se pisaba el título **y nada más**: la sala, el año y el elenco de la propuesta anterior seguían ahí y se enviaban. El admin recibiría "Macbeth" con la sala de "Hamlet", que **es peor que no recibir nada** — el catálogo cerrado (D7) existe para que el dato sea confiable. Ahora un título distinto empieza de cero; el mismo título —volver del login— conserva todo. **(3) El `401` al borrar no dejaba la marca que reabre la hoja.** Crear y editar sí lo hacían. **Las tres inconsistencias**: los dos buscadores (cabecera y 404) no declaraban `method`, que es la regla que D90 acababa de escribir —el default era el correcto, pero una regla que la mitad de los formularios no cumple deja de ser una regla—; el alta no validaba el máximo de 254 del email que el contrato fija; y **`SCREEN_SPECS.md` decía que con sesión el login redirige a la home mientras el código redirige al `?volver=`**. **Gana el código y se corrige el documento**: el único caso en que esto pasa de verdad es una pestaña vieja apuntando al login con la sesión ya viva, y ahí tirar el destino que la propia URL trae es perder el "volver a donde estaba" que esa pantalla existe para cumplir. Sin `?volver=`, la home, como decía la regla. **El pendiente que se cierra**: D83 había dejado anotado que **cuando existiera `Aviso` habría que decidir si un `yo()` caído merece una banda**. `Aviso` existe desde este paso, así que la respuesta ya no puede ser "no hay con qué dibujarla": **por ahora no se dibuja**, y se revisa en el paso 5 — **D83 no se reescribe**: queda como está y esta entrada la cierra por fecha. **Y las cuatro referencias que la primera corrección dejó mal atribuidas**: el cierre del `?volver=`, el de la banda y el encabezado de `SCREEN_SPECS.md` decían D90 —la decisión que ya estaba escrita cuando se editaron— y son de esta; el ROADMAP nombraba una sola decisión del paso y son dos. Corregidas las cuatro | Las tres primeras son defectos y no diferencias de gusto, y las tres comparten forma: **un efecto corriendo detrás de otro**. Vale decirlo porque es el tipo de error que este frontend va a repetir — el borrador que se guarda solo es cómodo justamente porque nadie lo llama, y por eso hay que mirarlo cuando algo más escribe el mismo estado. La (1) además **sólo se ve mirando `sessionStorage` en un navegador**: leyendo el código las dos líneas parecen decir lo que había que decir. **Por qué la banda de sesión se difiere y no se implementa** (el pendiente de D83): para distinguir un fallo de un visitante anónimo —que es el caso normal—, `yo()` tendría que devolver algo más que `Cuenta | null`, y eso lo pagan sus cuatro llamadores para un estado que **se corrige solo** (cualquier acción protegida se come su `401` y va al login); además la banda caería sobre las páginas públicas que Next todavía puede servir de su caché, que es justo lo que esa degradación protege, y "no pudimos verificar tu sesión" no es accionable. **En el paso 5 el fallo pasa a ser visible de verdad** —con el feed escrito, un `yo()` caído le muestra la landing del visitante a alguien logueado— y ahí se decide con el caso a la vista. **El autocompletado: la objeción era correcta y la primera respuesta no.** Se había rechazado que "permita elegir resultados de la consulta anterior" con un argumento que cubría **una sola de las dos ventanas** —la del pedido en vuelo, donde efectivamente el dedo está quieto porque la consulta sale recién 250 ms después de la última tecla—. **La otra ventana son esos mismos 250 ms**, y ahí pasa justo lo contrario: `texto` ya cambió, `consulta` todavía no salió, `buscando` sigue en `false`, así que **la lista vieja no estaba ni atenuada ni anunciada** y `Enter` elegía sobre ella. Escribir "mac" sobre "ham" y apretar `Enter` antes de los 250 ms registraba Hamlet. La re-auditoría lo señaló con el caso reproducible y tiene razón. **Y hay una tercera, que encontró la re-auditoría de esa misma corrección**: el cuadro que se pinta cuando el temporizador mueve `consulta` y el efecto todavía no arrancó el pedido —ahí `texto === consulta`, el `buscando` seguía en `false` y los ítems eran los de la consulta anterior, porque **un efecto que no nace de una interacción corre después del pintado**—. Las tres salen de la misma causa y por eso se arreglan una sola vez: **la vigencia se estaba comparando contra la pregunta (`consulta`) y no contra la respuesta que está en pantalla**. **Los ítems pasan a viajar con la consulta de la que salieron**, en un solo estado, y la señal es `texto !== lista.consulta`; `buscando` desaparece, que era la misma pregunta hecha tarde y por otro lado. El invariante deja de depender del orden de los efectos y pasa a ser estructural: **no existe un render donde los ítems sean de A y la consulta de la lista diga B**, porque se escriben juntos. Con esa señal se atenúan la lista, el punto del campo y el anuncio del `aria-live`. ⚠️ **Y la evidencia de esta tercera hay que decirla como es, porque no es la de las otras**: no se pudo reproducir. Un muestreo cuadro por cuadro con la búsqueda demorada a propósito —152 cuadros— y un barrido de `Enter` milisegundo a milisegundo alrededor del límite de los 250 ms —40 intentos— **pasan igual con el código viejo que con el nuevo**: en Chromium, React alcanza a correr el efecto antes del pintado y antes del `Enter` siguiente. Lo que sostiene el cambio no es una medición sino que **la corrección anterior dependía de un orden que React no promete** (la documentación es explícita en que un efecto puede correr después del pintado), y una regla que se cumple por casualidad no es una regla. El cambio además saca un estado en vez de agregarlo, así que el costo de cerrarla es negativo Y se agrega lo que la atenuación sola no arregla: **`Enter` con la lista desactualizada no elige, adelanta la búsqueda**. Es el único camino en el que la persona **no apuntó a una fila** —está confiando en "el primero"—, y por eso el clic y el toque siguen eligiendo igual: ahí sí se apuntó a algo que está a la vista. Lo que **no** se hace es vaciar la lista en cada consulta: la haría parpadear a vacío en cada pausa de tipeo, que es lo contrario de los seis toques de P8 |

## Supuestos (hipótesis, no requisitos)

| ID | Supuesto | Evidencia | Riesgo si es falso |
|----|----------|-----------|--------------------|
| S1 | Existe un segmento de espectadores intensivos en CABA que quiere registrar y compartir lo que ve, y sus soluciones actuales (Instagram + memoria + entradas) le resultan insuficientes | Conversaciones informales con un puñado de amigos/conocidos espectadores. Sin validación formal, por decisión del fundador | El producto no tiene demanda. Contramedida: MVP mínimo |
| S2 | La capa social mínima es necesaria desde el día uno para la adopción | Intuición del fundador; la contraparte técnica hubiera pospuesto lo social | Se construye capa social para una plataforma sin masa crítica |
| S3 | El comportamiento de los usuarios objetivo (publicar en Instagram, seguir influencers teatrales) se traslada a una plataforma estructurada | Observación indirecta | El hábito no migra; el registro no compite con la story |

## Descartado por ahora

| ID | Descarte | Razón |
|----|----------|-------|
| X1 | Gamificación para convertir espectadores ocasionales en intensivos | Hipótesis sin evidencia; la identidad "teatrera" no la crea una app. Estadísticas personales simples sí entran como parte del diario |
| X2 | Recomendación algorítmica | Complejidad sin masa de datos que la justifique |
| X3 | Competir con Instagram en lo visual/efímero | Explorar a futuro integración (compartir logs como imagen para stories) en lugar de competir |
| X4 | Agenda de funciones con fechas y horarios | Dato que caduca a diario; deuda operativa insostenible para una persona (ver principio P6). Posiblemente nunca se incorpore |
| X5 | Comentarios en reseñas (en MVP) | Ver D11 |

## Pendientes

| ID | Pendiente | Etapa donde se resuelve |
|----|-----------|------------------------|
| P1 | Entrevistas con 5-10 espectadores intensivos de CABA | Recomendado antes de congelar el MVP; pospuesto por decisión del fundador |
| P2 | Nombre del producto | Libre |
| P15 | MD-5: ¿cambio de contraseña en el MVP o post-MVP? (única "configuración" candidata; sin email transaccional, el "olvidé mi contraseña" es otro problema encadenado) | Antes de Fase 1 |
| P18 | **El `notFound()` y el `error.tsx` de una pantalla del sitio sólo se pintan con JavaScript.** Medido en el paso 2 de la Fase 4, con `next start` y el navegador sin JS: `/ruta-inexistente` (el 404 global) **sí llega en el HTML**, pero `notFound()` llamado desde la ficha, el artista o la sala responde `404` con `<html id="__next_error__">` y **el cuerpo vacío**; el contenido viaja en el payload RSC y lo pinta el cliente al hidratar. Lo mismo el `500` de `error.tsx`. Aislado con sondas: **no es el `error.tsx`** (se sacó y siguió igual), **no es el `not-found.tsx` de `(sitio)`** (ídem), **y no es que la ruta sea dinámica** —una ruta de prueba con `cookies()` y `notFound()` **fuera** de `(sitio)` sí se renderiza en el HTML—. Lo que queda apuntado es **el layout `async` del armazón por encima del boundary**. Impacto real, acotado: el estado HTTP es correcto (`404`/`500`), con JavaScript se ve la pantalla entera con sus salidas, y un preview de WhatsApp de un link roto no tiene nada que mostrar igual; lo que se pierde es la 404 sin JavaScript. **No lo arregla este paso**: falta confirmar si es comportamiento esperado de Next 16 o un defecto, y cualquier rodeo tocaría el armazón, que es de D81/D83 | Fase 4, antes de la beta: se revisa al escribir el paso 5 (que agrega islas y vuelve a tocar el armazón) o cuando suba la versión de Next |
| ~~P16~~ | ✅ **Resuelto en D88**, con el endpoint escrito en el mismo paso. El enunciado original y su estado intermedio: **la herramienta y el formato los decidió el fundador** —TwelveMonkeys (`imageio-webp` + `imageio-jpeg`, Java puro, sin binarios nativos) para decodificar, y **salida JPEG**, porque no existe un escritor de WebP en Java puro y el JDK solo escribe JPEG y PNG—. Eso **cambia la URL pública de `.webp` a `.jpg` y obliga a enmendar D77**, y y se escribió como D88 junto con el endpoint. Lo que quedaba abierto —calidad, orientación EXIF y tope de píxeles decodificados— también lo cierra D88. El enunciado original: **cómo se procesa el afiche al subirlo**: con qué se decodifica/redimensiona/codifica (el JDK no escribe WebP y el `pom.xml` no tiene ninguna librería de imágenes, así que hay dependencia nueva a decidir como se decidió Tailwind en D73 — o cambiar el formato de salida y no depender de nada), a qué dimensiones y con qué calidad, qué se hace con la orientación EXIF, y qué tope de **píxeles decodificados** además del de 5 MB de archivo, que no acota la memoria. D77 fijó el contrato HTTP y el versionado; esto es lo que le falta para poder implementarse. ✅ **La parte que dependía del diseño ya está**: D79 fijó las dimensiones en las que se muestra un afiche —lado mayor mostrado 1200 px (el `og:image`), caja de origen de 1200×1600 **sin recortar**, un solo archivo, el recorte de la grilla lo hace CSS—. Lo que sigue abierto es lo demás: herramienta, formato, calidad, EXIF y tope de píxeles decodificados | **Fase 4, ya desbloqueado por `DESIGN_SYSTEM.md`** (D79). Bloquea la subida de afiches, no las pantallas |
| ~~P17~~ | **Resuelto en D84: ganó el infinitivo.** Se corrigió la regla de voz de `DESIGN_SYSTEM.md`; las etiquetas de `SCREEN_SPECS.md` ya estaban bien y no se tocaron. El enunciado original: **MD-6: ¿los botones van en imperativo voseante o en infinitivo?** `DESIGN_SYSTEM.md` fija en "Voz y microcopy" que **los botones son verbos en imperativo voseante** —"Registrá", "Sugerí", "Publicá", "Seguí"— con una sola excepción, el panel admin, que va en infinitivo seco porque es una herramienta (Flujo 4). `SCREEN_SPECS.md` nombra las etiquetas de producto **en infinitivo**: "Registrar", "Publicar", "Registrar que la vi", "Registrar de nuevo". **Hoy se están usando las de `SCREEN_SPECS.md`** —es el documento más nuevo y es el que se lee al sentarse a escribir una pantalla—, así que la regla de la voz está escrita en un lado y desmentida en el otro. Hay que **cerrar cuál gana y dejar las dos fuentes en el mismo idioma**: si gana el imperativo, se reescriben las etiquetas de `SCREEN_SPECS.md`; si gana el infinitivo, se corrige la regla de `DESIGN_SYSTEM.md` y se dice por qué (el infinitivo es más corto, que en una barra de 82 px no es un detalle). Lo encontró la revisión de D81, que agregó etiquetas nuevas —"Menú", "Mi diario", "Salir", "Entrar"— y tuvo que elegir sin que la regla estuviera cerrada | **Cerrado en el paso 1 de la Fase 4**, que es donde estaba pedido |
| ~~P3~~ | **Resuelto**: ~50 producciones impecables (D38). Lista concreta de títulos: tarea pre-lanzamiento | Cerrado en Etapa 7 |
| ~~P4~~ | **Resuelto en diferido**: escalera de fuentes en DATA_STRATEGY.md; el scraper es el peldaño 2, con revisión legal en su momento (D39) | Cerrado en Etapa 7 |
| ~~P5~~ | **Resuelto**: listas fuera del MVP (D25) | Cerrado en Etapa 4 |
| ~~P6~~ | **Resuelto**: reemplazos/reestrenos los decide el admin caso por caso, con sesgo a "misma producción" si se mantienen texto y dirección | Cerrado en Etapa 2 |
| ~~P7~~ | **Resuelto**: rating 1–10 confirmado (D9, D18) | Cerrado en Etapa 3 |
| ~~P8~~ | **Resuelto**: ver `docs/domain/GLOSSARY.md` y `docs/domain/DOMAIN_MODEL.md` | Cerrado en Etapa 2 |
| P9 | Modelado de complejos multi-sala (campo texto vs auto-relación) | Diseño de esquema |
| ~~P10~~ | **Resuelto**: ver D18–D21 y `docs/product/CORE_LOOP.md` | Cerrado en Etapa 3 |
| ~~P11~~ | **Resuelto**: AGPL-3.0 (D46) | Cerrado en Etapa 9 |
| ~~P12~~ | **Resuelto**: ver D48 | Cerrado en Etapa 9 |
| ~~P13~~ | **Resuelto**: Next.js (D43, ADR-003) | Cerrado en Etapa 8 |
| ~~P14~~ | **Resuelto**: afiches en disco local con volumen (D45, contrato en D77). ⚠️ **Ojo con la lectura fácil de esto: el `pg_dump` → R2 de D45 respalda PostgreSQL y NO el volumen de afiches.** Son dos backups separados, y los dos se restauran de prueba antes de la beta (D77) | Cerrado en Etapa 8; la parte del backup del volumen se corrigió al escribir D77 |
